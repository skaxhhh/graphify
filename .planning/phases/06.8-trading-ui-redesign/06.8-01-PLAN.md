---
phase: 06.8-trading-ui-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/trading/ui/TradeButton.tsx
  - frontend/src/components/trading/ui/TradeBadge.tsx
  - frontend/src/components/trading/ui/TradeCard.tsx
  - frontend/src/components/trading/ui/TradeStatCard.tsx
  - frontend/src/components/trading/ui/TradeTable.tsx
  - frontend/src/components/trading/ui/TradePageState.tsx
  - frontend/src/components/trading/ui/TradeInput.tsx
  - frontend/src/components/trading/ui/TradeModeIndicator.tsx
  - frontend/src/components/trading/ui/index.ts
  - frontend/src/layouts/TradingLayout.tsx
  - frontend/src/components/trading/PaperTradingToggle.tsx
  - frontend/src/router/index.tsx
autonomous: true
requirements: [UIX-01]
user_setup: []

must_haves:
  truths:
    - "components/trading/ui/ exports 8 trade primitives that every downstream /trading screen can import"
    - "TradingLayout renders the Binance dark shell: trade-bg page floor, trade-surface sidebar, top mode indicator, bottom 모의/실거래 segmented toggle"
    - "Sidebar PAPER group shows D4 labels (전략 설정 / 전략 운영) and includes 동작 모니터링 (D3)"
    - "monitor route is reachable in PAPER mode (ModeGuard mode=PAPER), not LIVE"
    - "trade-* tokens appear only under /trading; cream app routes are untouched"
  artifacts:
    - path: "frontend/src/components/trading/ui/index.ts"
      provides: "Barrel export of all 8 trade primitives"
      exports: ["TradeButton", "TradeBadge", "TradeCard", "TradeStatCard", "TradeTable", "TradePageState", "TradeInput", "TradeModeIndicator"]
    - path: "frontend/src/components/trading/ui/TradePageState.tsx"
      provides: "loading/empty/error states using trade tokens (replaces cream EmptyState/ErrorBanner/SkeletonBlock tone=dark)"
      min_lines: 25
    - path: "frontend/src/layouts/TradingLayout.tsx"
      provides: "Reskinned shell with mode indicator + segmented toggle + D3/D4 nav"
      contains: "TradeModeIndicator"
    - path: "frontend/src/router/index.tsx"
      provides: "monitor route under ModeGuard mode=PAPER"
      contains: "monitor"
  key_links:
    - from: "frontend/src/layouts/TradingLayout.tsx"
      to: "frontend/src/components/trading/ui/index.ts"
      via: "import trade primitives"
      pattern: "components/trading/ui"
    - from: "frontend/src/router/index.tsx"
      to: "TradingMonitorPage"
      via: "ModeGuard mode=PAPER"
      pattern: "monitor"
---

<objective>
Establish the Wave 1 foundation for the Phase 6.8 Binance dark reskin: build the trade-only primitive catalog under `components/trading/ui/`, rebuild the `TradingLayout` shell to match `wireframe.html` (mode indicator + segmented mode toggle + reorganized nav), reskin `PaperTradingToggle`, and apply the router IA change (D3 monitor → PAPER). Every later wave (02–05) imports these primitives, so this plan is the dependency root.

Purpose: A single, token-correct primitive layer means waves 2–5 become mechanical token application with zero ad-hoc styling. The shell + IA changes (D3/D4/D8) are the structural backbone the wireframe is built around.
Output: 8 new primitive components + barrel export, a reskinned layout shell, a reskinned mode toggle, and the monitor-route IA fix — all behavior-preserving.

Behavior unchanged: No backend/REST/DTO edits. No chart-component edits. tradingStore, ModeGuard logic, and PaperTradingToggle's optimistic-update/rollback flow are preserved exactly — only presentation changes.
</objective>

<execution_context>
@/Users/heojeonghun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heojeonghun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.8-trading-ui-redesign/06.8-CONTEXT.md
@.planning/phases/06.8-trading-ui-redesign/06.8-RESEARCH.md
@.planning/phases/06.8-trading-ui-redesign/06.8-VALIDATION.md
@wireframe.html
@DESIGN-binance.md

<interfaces>
<!-- trade-* token namespace (frontend/tailwind.config.js, already done) — use ONLY these, no hardcoded hex -->
Colors: trade-primary (#fcd535), trade-primary-active, trade-primary-disabled,
        trade-bg (#0b0e11 page floor), trade-surface (#1e2329 cards/sidebar),
        trade-elevated (#2b3139 nested/hover), trade-hairline (#2b3139 borders),
        trade-ink (#181a20 text-on-yellow), trade-body (#eaecef default text),
        trade-muted (#707a8a captions), trade-muted-strong (#929aa5),
        trade-on-dark (#ffffff headings),
        trade-up (#0ecb81 gain/BUY text), trade-down (#f6465d loss/SELL text),
        trade-up-soft (#0e1f1a badge bg), trade-down-soft (#21141a badge bg),
        trade-info (#3b82f6 focus ring).
Fonts: font-trade-sans (Inter — UI/body), font-trade-mono (JetBrains Mono — numbers/prices ONLY).

<!-- tradingStore (frontend/src/stores/tradingStore.ts) — DO NOT MODIFY -->
useTradingStore: { mode: "PAPER"|"LIVE", setMode(m), disableDarkMode() }

<!-- ModeGuard (frontend/src/components/trading/ModeGuard.tsx) — DO NOT MODIFY logic -->
<ModeGuard mode="PAPER"|"LIVE">{children}</ModeGuard>  // redirects on mismatch

<!-- Current nav arrays in TradingLayout (to be reorganized per D3/D4) -->
commonItems: DDS Agent (/trading, end), 토스 설정 (/trading/settings)
paperItems target (wireframe order): 모의 대시보드, 전략 설정 (/paper/rules),
  전략 운영 (/paper/rules-lifecycle), 백테스트, 동작 모니터링 (/trading/monitor),
  모의 거래 이력, 모의 성과 리포트
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the trade primitive catalog (components/trading/ui/)</name>
  <files>
    frontend/src/components/trading/ui/TradeButton.tsx,
    frontend/src/components/trading/ui/TradeBadge.tsx,
    frontend/src/components/trading/ui/TradeCard.tsx,
    frontend/src/components/trading/ui/TradeStatCard.tsx,
    frontend/src/components/trading/ui/TradeTable.tsx,
    frontend/src/components/trading/ui/TradePageState.tsx,
    frontend/src/components/trading/ui/TradeInput.tsx,
    frontend/src/components/trading/ui/TradeModeIndicator.tsx,
    frontend/src/components/trading/ui/index.ts
  </files>
  <action>
Create 8 trade-only primitives + a barrel index. Use the implementations in 06.8-RESEARCH.md "Pattern 1" verbatim as the baseline for TradeButton, TradeStatCard, TradePageState, TradeBadge; build the rest in the same style. Token-only — NO hardcoded hex, NO cream classes (bg-gray-*, text-gray-*, emerald-*, border-white/*).

- TradeButton: variants primary | secondary | danger | ghost; sizes sm | md; disabled + loading. primary = bg-trade-primary text-trade-ink hover:bg-trade-primary-active disabled:bg-trade-primary-disabled disabled:text-trade-muted; secondary = bg-trade-surface text-trade-body border border-trade-hairline hover:bg-trade-elevated; danger = bg-trade-down text-trade-on-dark; ghost = bg-transparent text-trade-body hover:bg-trade-elevated. focus:ring-2 focus:ring-trade-info.
- TradeBadge: variants active | draft | running | stopped | up | down | info | warning per RESEARCH badgeStyles map (soft bg + colored text; e.g. active/running = bg-trade-up-soft text-trade-up, draft/stopped = bg-trade-elevated text-trade-muted border-trade-hairline).
- TradeCard: rounded-lg bg-trade-surface border border-trade-hairline p-4; optional title prop (text-sm font-medium text-trade-body) + children.
- TradeStatCard: { label, value, sub?, valueColor?: "up"|"down"|"neutral" } — value uses font-trade-mono; up=text-trade-up, down=text-trade-down, neutral=text-trade-on-dark; label text-xs text-trade-muted.
- TradeTable: thin wrapper — rounded-lg bg-trade-surface border border-trade-hairline overflow-hidden; exports TradeTable + TradeTableRow (grid row, border-b border-trade-hairline, hover:bg-trade-elevated) + TradeTableHeader (text-xs text-trade-muted). Keep it layout-agnostic (consumers pass grid-template via className) so existing table grids drop in unchanged.
- TradePageState: variant loading | empty | error with { title?, message?, onRetry?, className? } exactly per RESEARCH example. CRITICAL: message/title strings are passed by consumers — this component must render whatever Korean strings callers give (so existing e2e text assertions stay green).
- TradeInput: text input — bg-trade-surface text-trade-body border border-trade-hairline rounded-md px-3 h-10 placeholder:text-trade-muted focus:ring-2 focus:ring-trade-info focus:outline-none; forward className + standard input props; optional font-trade-mono toggle via a `mono?: boolean` prop for numeric fields.
- TradeModeIndicator (D8): reads useTradingStore mode; renders the wireframe "mode indicator" card (bg-trade-surface rounded-lg p-3): a status dot (trade-primary for PAPER, trade-down for LIVE) + "PAPER 모드"/"실거래 모드" (font-trade-sans) + sub caption ("모의 투자 · 가상 체결" / "실계좌 · 실주문") in text-trade-muted.
- index.ts: re-export all 8 components (named exports).

Source of truth for visuals: wireframe.html sidebar block (lines ~58–95) and DESIGN-binance.md button/badge/card component specs.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    grep guard (must return 0 hits): grep -rn "#[0-9a-fA-F]\{3,6\}\|bg-gray-\|text-gray-\|emerald-\|border-white/" frontend/src/components/trading/ui/
  </verify>
  <done>8 primitive files + index.ts exist and compile; tsc -b + vite build pass; no hardcoded hex or cream classes in components/trading/ui/.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild TradingLayout shell + reskin PaperTradingToggle + router IA (D3/D4/D8)</name>
  <files>
    frontend/src/layouts/TradingLayout.tsx,
    frontend/src/components/trading/PaperTradingToggle.tsx,
    frontend/src/router/index.tsx
  </files>
  <action>
Do all three in one commit (Pitfall 3: router monitor change and layout nav change must ship together or the monitor link disappears).

TradingLayout.tsx (reskin + relayout, preserve the server-mode-sync useEffect and handleExit exactly):
- Root: bg-gray-950 → bg-trade-bg; add font-trade-sans on the root div (Inter for all UI). text-white → text-trade-body.
- Sidebar aside + mobile drawer aside: bg-gray-900 → bg-trade-surface, border-white/10 → border-trade-hairline.
- SidebarHeader: "graphify" wordmark text-trade-primary (font-trade-sans, bold); BETA badge → border-trade-primary/40 text-trade-primary (per wireframe the BETA chip is yellow-outlined).
- Insert <TradeModeIndicator /> at the top of the sidebar (below header) per D8.
- SidebarNav: render group labels "PAPER"/"LIVE" + "공통" (text-xs font-semibold text-trade-muted) like the wireframe. Active NavLink: bg-trade-surface/elevated + left border-l-2 border-trade-primary + text-trade-primary; inactive: text-trade-muted hover:bg-trade-elevated hover:text-trade-body.
- Update nav arrays per D3 + D4:
    - liveItems: 대시보드 (/trading/dashboard), 거래 이력 (/trading/history), 전략 운영 (/trading/rules)  [D4: "현재 룰"→"전략 운영"; REMOVE the monitor entry and the standalone "룰 수정" entry].
    - paperItems (wireframe order): 모의 대시보드 (/trading/paper/dashboard), 전략 설정 (/trading/paper/rules) [D4], 전략 운영 (/trading/paper/rules-lifecycle) [D4], 백테스트 (/trading/paper/backtest), 동작 모니터링 (/trading/monitor) [D3 — moved into PAPER], 모의 거래 이력 (/trading/paper/history), 모의 성과 리포트 (/trading/paper/report).
- Header bar: bg-gray-900 → bg-trade-surface, border + text tokens swapped; "BETA" chip → trade-primary outline. "메인으로" button → text-trade-muted hover:text-trade-body.
- SidebarFooter: keep PaperTradingToggle; swap "메인으로" tokens; border-t border-trade-hairline.

PaperTradingToggle.tsx (reskin only — preserve applyMode/optimistic/rollback + confirmLive flow exactly):
- Add the wireframe segmented 모의/실거래 control look OR keep the existing toggle but reskinned — minimum: container border-t border-trade-hairline; label text-trade-body; status text isPaper ? text-trade-up : text-trade-down; toggle on = bg-trade-primary, off = bg-trade-elevated; knob bg-trade-on-dark.
- Confirm modal: overlay bg-black/60; panel bg-trade-surface border-trade-hairline; title text-trade-on-dark; body text-trade-muted with the risk sentence; Cancel = TradeButton variant=secondary; Confirm CTA = TradeButton variant=primary (YELLOW per D8 / wireframe line 169 "실거래로 전환" uses #fcd535 — NOT red). Keep the exact Korean strings.

router/index.tsx (D3 — single logical change):
- Change the `monitor` route's <ModeGuard mode="LIVE"> → <ModeGuard mode="PAPER">. Leave path "monitor" and all other routes unchanged. (FALLBACK map in ModeGuard stays untouched.)
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    grep guard (0 hits): grep -rn "#[0-9a-fA-F]\{3,6\}\|bg-gray-\|text-gray-\|emerald-\|bg-white/\|border-white/" frontend/src/layouts/TradingLayout.tsx frontend/src/components/trading/PaperTradingToggle.tsx
    verify monitor guard: grep -n "monitor" frontend/src/router/index.tsx (the monitor route block must read ModeGuard mode="PAPER")
  </verify>
  <done>Build green. Sidebar shows mode indicator + D4 labels + 동작 모니터링 under PAPER + segmented/reskinned toggle. monitor route guarded by PAPER. No cream classes/hex remain in the 3 files. PaperTradingToggle optimistic+rollback+confirm logic unchanged.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` (tsc -b + vite) passes.
- Token guards (VALIDATION.md c-1/c-2): 0 hardcoded hex, 0 cream classes in `components/trading/ui/`, `TradingLayout.tsx`, `PaperTradingToggle.tsx`.
- Token-leak guard (c-3): `grep -rn "trade-" frontend/src/pages/ frontend/src/components/ frontend/src/layouts/ --include="*.tsx" | grep -v "/trading/" | grep -v "components/trading/"` → 0 hits (TradingLayout is under /layouts but imports from /trading; that's expected — the leak guard targets cream pages/components).
- Visual (browse): open /trading — sidebar matches wireframe shell (mode indicator, PAPER group order, segmented toggle, yellow brand). Switch mode → monitor link present in PAPER.
- cream non-regression: visit /app/history and /companies/:id — cream theme intact.
</verification>

<success_criteria>
SC1 (tokens), SC2 (ModeGuard/toggle behavior preserved), SC5 (cream non-regression), SC6 (monitor→PAPER, D4 labels), SC8/D8 (mode indicator + segmented toggle + yellow LIVE-confirm CTA) all satisfied for the shell. Primitives ready for waves 2–5.
</success_criteria>

<output>
After completion, create `.planning/phases/06.8-trading-ui-redesign/06.8-01-SUMMARY.md` documenting the primitive API surface (props of each component) so waves 2–5 import without re-reading source.
</output>
