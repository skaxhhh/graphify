---
phase: 06.8-trading-ui-redesign
plan: "04"
subsystem: frontend-trading-ui
tags: [backtest-page, Binance-dark-theme, TradeStatCard, TradeCard, TradeInput, TradeButton, CandleSection, EquityCurveChart, TradingCompanyPickerModal]
dependency_graph:
  requires:
    - 06.8-01 (trade primitives: TradeButton, TradeCard, TradeInput, TradeStatCard)
    - 06.8-03 (CandleSection state-swap already applied — reused as-is)
  provides:
    - PaperBacktestPage Binance dark reskin (form + 5 metrics + equity + advanced stats + candle + trade table)
  affects: []
tech_stack:
  added: []
  patterns:
    - TradeInput for date/time/number inputs; native select with trade tokens (bg-trade-bg/border-trade-hairline)
    - TradeStatCard for all numeric metric cards (valueColor up/down/neutral)
    - TradeCard wraps EquityCurveChart (D2 reuse) and CandleSection (D2 reuse)
    - TradeButton primary for run action; disabled={isPending || ruleId === ""} preserved
    - Native table inside trade-surface container (tr/td DOM structure preserved for e2e)
    - TradingCompanyPickerModal replaces shared/CompanyPickerModal (import swap, props byte-identical)
key_files:
  created: []
  modified:
    - frontend/src/pages/trading/paper/PaperBacktestPage.tsx
decisions:
  - "Select (rule drop-down) styled inline with trade tokens (bg-trade-bg border-trade-hairline h-10) — TradeInput wraps <input> only, not <select>"
  - "TradeButton primary without loading prop — children text conditional (실행 중.../백테스트 실행) preserves original Korean string byte-identical"
  - "TradeCard used as plain wrapper (no title prop) for equity curve — custom flex header inside children provides title-left/sub-right layout matching wireframe"
  - "CandleSection wrapped in TradeCard for trade-surface backdrop — CandleSection internals untouched (D2 diff = 0)"
  - "CompanyPickerModal (shared/) → TradingCompanyPickerModal (06.8-02) import path swap; props byte-identical; shared/ untouched (D6)"
  - "Removed inline Metric/StatCard helper components — replaced by TradeStatCard"
metrics:
  duration: "2m"
  completed_date: "2026-06-28"
  tasks_completed: 2
  files_created: 0
  files_modified: 1
---

# Phase 6.8 Plan 04: Backtest Page Reskin Summary

**One-liner:** PaperBacktestPage fully reskinned to Binance dark theme — TradeInput form + 5 TradeStatCard metrics + EquityCurveChart in TradeCard + CandleSection in TradeCard (D2) + native trade-token table + TradingCompanyPickerModal swap, all run logic and Korean strings preserved.

## Changes Applied

### Page Header
- `text-white` → `text-trade-on-dark`
- `text-gray-400` → `text-trade-muted`
- Sub-text updated to wireframe wording: "5분봉 인트라데이 (09:00–12:00 KST · 최대 60일) 검증"

### Input Form (TradeCard-style container)
- Container: `rounded-lg border border-trade-hairline bg-trade-surface p-4`
- Rule select: inline trade tokens (`bg-trade-bg border-trade-hairline h-10 font-trade-sans`)
- Date inputs (시작일/종료일): `TradeInput type="date"`
- Time inputs (시간대 from/to): `TradeInput type="time"` with `–` separator
- Initial capital (초기 자본): `TradeInput mono type="number"`
- Run button: `TradeButton variant="primary"` disabled when `ruleId === ""` or `isPending`
- Error message: `text-trade-down`

### 5 Metric Cards
| Card | valueColor |
|------|-----------|
| 최종 평가액 | neutral |
| 수익률 | up (≥0) / down (<0) |
| MDD | down |
| 승률 | neutral |
| 거래수 | neutral |

### Equity Curve (D2 Reuse)
- `TradeCard` wrapper (no title prop); custom header inside: title left / sub-text right
- `EquityCurveChart` props unchanged: `{ data, drawdownSegments, initialCash }`
- D2 guard confirmed: EquityCurveChart.tsx diff = 0

### Advanced Statistics
- 3x `TradeStatCard` with `sub` prop for description text
- `Sharpe Ratio`, `Sortino Ratio`, `Profit Factor` — neutral valueColor

### CandleSection (D2 Reuse)
- `TradeCard` wrapper → trade-surface backdrop
- `CandleSection` props unchanged (symbol, date, trades, indicators, highlightTime, highlightSide)
- First-trade auto-select (`onSuccess` useEffect) preserved exactly
- D2 guard confirmed: CandleSection.tsx and CandleChart.tsx diff = 0

### Trade Table
- Container: `rounded-lg border border-trade-hairline bg-trade-surface overflow-hidden`
- `overflow-x-auto` inner wrapper for mobile
- Header row: `border-trade-hairline text-trade-muted font-trade-sans`
- Data rows: `border-trade-hairline/50 hover:bg-trade-elevated transition-colors`
- 일시 (KST): `text-trade-muted font-trade-mono`
- 종목: `text-trade-on-dark`
- 구분 (BUY/SELL): `text-trade-up` / `text-trade-down`
- 수량/가격: `text-trade-muted font-trade-mono`
- 손익 (PnL): `text-trade-up` (>0) / `text-trade-down` (<0) / `text-trade-muted` (null) + `font-trade-mono`
- Rationale accordion row: `bg-trade-elevated/40`
- Empty state string preserved: "체결된 거래가 없습니다."
- Click-to-expand accordion behavior preserved exactly

### Empty-Universe Fallback
- `CompanyPickerModal` (shared/) → `TradingCompanyPickerModal` (trading/)
- Props byte-identical: `{ open, title, description, confirmLabel, onClose, onConfirm }`
- ERR_BACKTEST_UNIVERSE_EMPTY handler and mutation.mutate(symbols) flow preserved exactly

## Verification Results

| Check | Result |
|-------|--------|
| `npm run build` — Task 1 | PASS |
| `npm run build` — Task 2 | PASS |
| Token guard (0 hex, 0 cream, 0 gray-/emerald-/white/) — both tasks | PASS |
| D2 guard — EquityCurveChart.tsx diff | PASS (diff = 0) |
| D2 guard — CandleSection.tsx / CandleChart.tsx diff | PASS (diff = 0) |
| ruleId === "" → button disabled | PRESERVED |
| Auto-select first trade on result | PRESERVED |
| Korean strings byte-identical | PASS |

## Commits

| Hash | Description |
|------|-------------|
| `dae741a` | feat(06.8-04): reskin backtest input form + metrics + equity chart |
| `7e5bbc1` | feat(06.8-04): reskin candle section + trade table + empty-universe modal swap |

## Deviations from Plan

**1. [Rule 2 - Enhancement] Trade table themed in Task 1 commit (not Task 2)**
- **Found during:** Task 1 implementation
- **Issue:** Trade table styling was within the same result block — separating into a strict Task 1 / Task 2 would produce a file with partial token violations between commits
- **Fix:** Included trade table theming in the Task 1 write for a consistently clean file; Task 2 then added only the CandleSection wrapper and TradingCompanyPickerModal swap
- **Impact:** No behavioral or functional change; the two-commit structure is maintained

## Self-Check: PASSED

- `frontend/src/pages/trading/paper/PaperBacktestPage.tsx` — modified (trade tokens, no cream/gray/emerald)
- Commit dae741a — verified in git log
- Commit 7e5bbc1 — verified in git log
- Build GREEN (both commits)
- Token guard CLEAN (0 violations)
- D2 guard CLEAN (EquityCurveChart, CandleSection, CandleChart diff = 0)
