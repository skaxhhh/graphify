---
phase: 06.8-trading-ui-redesign
plan: 04
type: execute
wave: 4
depends_on: ["06.8-01", "06.8-03"]
files_modified:
  - frontend/src/pages/trading/paper/PaperBacktestPage.tsx
autonomous: true
requirements: [UIX-01]
user_setup: []

must_haves:
  truths:
    - "백테스트 input form (룰 select, 시작/종료일, 시간대, 초기 자본, 실행 버튼) is themed and disabled when no rule selected"
    - "5 result metrics (최종 평가액, 수익률, MDD, 승률, 거래수) + advanced stats (Sharpe, Sortino, Profit Factor) render in trade stat cards"
    - "EquityCurveChart + CandleSection are reused (D2) on trade surfaces with first-trade auto-select"
    - "Trade table shows rationale accordion; empty-universe falls back to the trade stock-picker if currently wired"
    - "Backtest run/result behavior is unchanged — only theme/layout differs"
  artifacts:
    - path: "frontend/src/pages/trading/paper/PaperBacktestPage.tsx"
      provides: "Themed backtest page — form + 5 metrics + advanced stats + equity/candle reuse + trade table"
      contains: "EquityCurveChart"
  key_links:
    - from: "frontend/src/pages/trading/paper/PaperBacktestPage.tsx"
      to: "frontend/src/components/backtest/EquityCurveChart.tsx"
      via: "reused equity chart (D2)"
      pattern: "EquityCurveChart"
    - from: "frontend/src/pages/trading/paper/PaperBacktestPage.tsx"
      to: "frontend/src/components/backtest/CandleSection.tsx"
      via: "reused candle section (D2)"
      pattern: "CandleSection"
---

<objective>
Reskin the 백테스트 screen (PaperBacktestPage, 390 lines) to the Binance dark theme: input form, 5 result-metric cards, equity curve + candle section (reused, D2), advanced stats (Sharpe/Sortino/Profit Factor), trade table with rationale accordion, and the empty-universe stock-picker fallback. Matches wireframe section "백테스트".

Purpose: Backtest is the validation surface that wires together the two reused charts plus a results table — the densest single screen. The reskin keeps the run pipeline and result rendering identical.
Output: A trade-themed backtest page matching wireframe lines ~473–514.

Behavior unchanged: No backend/REST/DTO edits. EquityCurveChart, CandleSection (internal canvas), candleIndicators are reused, not modified (D2). BacktestRequest payload, run mutation, result mapping, and the first-trade auto-select are preserved. Korean strings (LOADING "실행 중…", EMPTY "체결된 거래가 없습니다.") stay byte-identical.
</objective>

<execution_context>
@/Users/heojeonghun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heojeonghun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.8-trading-ui-redesign/06.8-RESEARCH.md
@.planning/phases/06.8-trading-ui-redesign/06.8-01-SUMMARY.md
@.planning/phases/06.8-trading-ui-redesign/06.8-02-SUMMARY.md
@.planning/phases/06.8-trading-ui-redesign/06.8-03-SUMMARY.md
@wireframe.html
@frontend/src/pages/trading/paper/PaperBacktestPage.tsx

<interfaces>
<!-- Wave 1 primitives — see 06.8-01-SUMMARY.md -->
TradeButton, TradeStatCard(label,value,sub?,valueColor?), TradeCard(title?),
TradeInput(mono?), TradeTable/TradeTableRow/TradeTableHeader, TradePageState.

<!-- Reused charts (D2 — internals untouched). Wrapper swap: border-white/10 bg-gray-900/50 → border-trade-hairline bg-trade-surface -->
EquityCurveChart props: { data, drawdownSegments, initialCash }
CandleSection props: { symbol, date, trades, indicators, highlightTime?, highlightSide? }
  // CandleSection state-swap pattern already applied in 06.8-03 — reuse as-is.

<!-- Empty-universe fallback: TradingCompanyPickerModal from 06.8-02 (see 06.8-02-SUMMARY.md for import path).
     Only swap shared/CompanyPickerModal → TradingCompanyPickerModal IF the page already wires a picker.
     Do NOT add a new picker flow that did not exist (would be a behavior change). -->

<!-- Wireframe anchor: 백테스트 lines ~473–514. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin backtest input form + metrics + charts</name>
  <files>frontend/src/pages/trading/paper/PaperBacktestPage.tsx</files>
  <action>
Wireframe ~473–514. Preserve the run mutation + all state. Token-swap only.
- Header "백테스트" + sub "5분봉 인트라데이 (09:00–12:00 KST · 최대 60일) 검증".
- Input card (bg-trade-surface, 3-col grid): 룰 select, 시작일/종료일 (TradeInput date), 시간대, 초기 자본 (TradeInput mono); 백테스트 실행 → TradeButton primary, DISABLED when no rule selected (keep existing disabled condition), loading state during run.
- 5 metric cards (최종 평가액, 수익률, MDD, 승률, 거래수) → TradeStatCard; values font-trade-mono; 수익률 up, MDD down valueColor.
- 수익 곡선: EquityCurveChart reused inside TradeCard (title "수익 곡선", sub "드로우다운 음영 오버레이"); wrapper trade-surface; do NOT touch chart props/internals.
- Advanced stats (Sharpe, Sortino, Profit Factor) → 3 TradeStatCard (neutral).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    grep guard (0 hits): grep -rn "#[0-9a-fA-F]\{3,6\}\|bg-gray-\|text-gray-\|emerald-\|bg-white/\|border-white/" frontend/src/pages/trading/paper/PaperBacktestPage.tsx
    D2 guard: git diff --stat frontend/src/components/backtest/EquityCurveChart.tsx (no changes)
  </verify>
  <done>Build green; form + 5 metrics + advanced stats + equity chart themed; run-disabled-when-no-rule preserved; EquityCurveChart internals untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin candle section reuse + trade table (rationale) + empty-universe fallback</name>
  <files>frontend/src/pages/trading/paper/PaperBacktestPage.tsx</files>
  <action>
Continue in the same file (second commit).
- CandleSection reused (D2) inside a TradeCard; first-trade auto-select preserved. (CandleSection state theming already done in 06.8-03 — just consume it.)
- Trade table → TradeTable; columns per existing (시각/종목/구분/수량/가격/손익 or current schema); 구분/손익 colored via existing up/down helper (token swap only); numerics font-trade-mono. Rationale accordion → TradeRationaleRow (reskinned in 06.8-02) or inline trade tokens; keep "근거 정보 없음".
- Empty-universe fallback: IF the page currently opens a CompanyPickerModal when the universe resolves empty, swap that import to TradingCompanyPickerModal (06.8-02). If no such flow exists today, leave behavior as-is and just theme any existing empty-universe message (TradePageState / inline trade-muted). Do not introduce a new modal flow.
- States: LOADING "실행 중…", EMPTY "체결된 거래가 없습니다.", DISABLED messaging → trade tokens / TradePageState, strings byte-identical.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run test:e2e</automated>
    grep guard (0 hits): grep -rn "#[0-9a-fA-F]\{3,6\}\|bg-gray-\|text-gray-\|emerald-\|bg-white/\|border-white/" frontend/src/pages/trading/paper/PaperBacktestPage.tsx
    D2 guard: git diff --stat frontend/src/components/backtest/CandleSection.tsx frontend/src/components/backtest/CandleChart.tsx (no changes from this plan)
  </verify>
  <done>Build green; candle-chart.spec.ts GREEN; CandleSection reused (no internal edits here); trade table + rationale themed; empty-universe handling themed without behavior change; strings preserved.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build && npm run test:e2e` — build passes, candle-chart.spec.ts GREEN.
- Token guards (c-1/c-2): 0 hex, 0 cream classes in PaperBacktestPage.tsx.
- D2 guard: EquityCurveChart/CandleChart/candleIndicators unchanged by this plan.
- Visual (browse + visual-verdict): DEFAULT (form only), LOADING, EMPTY, success-with-results vs wireframe ~473–514.
</verification>

<success_criteria>
SC1 (tokens), SC2 (backtest run behavior preserved), SC3 (equity + candle reuse on dark surface), SC4 (DEFAULT/LOADING/EMPTY/DISABLED states + empty-universe fallback) satisfied for the backtest screen.
</success_criteria>

<output>
After completion, create `.planning/phases/06.8-trading-ui-redesign/06.8-04-SUMMARY.md`.
</output>
