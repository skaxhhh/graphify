---
phase: 00-data-infra-dynamic-universe
plan: 04
type: tdd
wave: 2
depends_on:
  - 00-01
  - 00-02
files_modified:
  - backend/src/main/java/com/graphify/trading/rule/definition/RuleDefinition.java
  - backend/src/main/java/com/graphify/trading/rule/definition/RuleDefinitionValidator.java
  - backend/src/main/java/com/graphify/trading/engine/MarketDataPort.java
  - backend/src/main/java/com/graphify/market/MarketBarRepository.java
  - backend/src/main/java/com/graphify/market/DbMarketDataAdapter.java
  - backend/src/main/java/com/graphify/trading/backtest/BacktestService.java
  - backend/src/test/java/com/graphify/trading/rule/definition/RuleDefinitionUniverseTest.java
  - backend/src/test/java/com/graphify/market/MarketBarRepositoryTopVolumeTest.java
autonomous: true
requirements:
  - DATA-03
  - DATA-04

must_haves:
  truths:
    - "volume_top_n 타입의 Universe JSON이 RuleDefinition으로 역직렬화된다"
    - "백테스트 실행 시 volume_top_n 유니버스는 날짜마다 거래량 상위 N 종목을 동적으로 선정한다"
    - "additionalSymbols에 명시된 종목이 동적 선정 종목과 합산된다"
    - "findTopVolumeSymbolsOnDate()가 지정 날짜의 in_kospi200=true 종목 중 거래량 내림차순으로 반환한다"
    - "기존 symbols 타입 유니버스 백테스트는 변경 없이 동작한다"
    - "RuleDefinitionValidator가 volume_top_n 타입을 허용한다"
  artifacts:
    - path: "backend/src/main/java/com/graphify/trading/rule/definition/RuleDefinition.java"
      provides: "Universe record에 market, topN, additionalSymbols 필드 추가"
      contains: "volume_top_n"
    - path: "backend/src/main/java/com/graphify/trading/rule/definition/RuleDefinitionValidator.java"
      provides: "volume_top_n 타입 허용"
      contains: "volume_top_n"
    - path: "backend/src/main/java/com/graphify/trading/engine/MarketDataPort.java"
      provides: "topVolumeSymbols() + symbolsByMarket() 메서드 추가"
      exports: ["topVolumeSymbols", "symbolsByMarket"]
    - path: "backend/src/main/java/com/graphify/market/MarketBarRepository.java"
      provides: "findTopVolumeSymbolsOnDate() + findDistinctKospi200Symbols() JPQL 쿼리"
      exports: ["findTopVolumeSymbolsOnDate", "findDistinctKospi200Symbols"]
    - path: "backend/src/main/java/com/graphify/market/DbMarketDataAdapter.java"
      provides: "topVolumeSymbols() 구현체"
      contains: "findTopVolumeSymbolsOnDate"
    - path: "backend/src/main/java/com/graphify/trading/backtest/BacktestService.java"
      provides: "volume_top_n 유니버스 날짜별 동적 선정 로직"
      contains: "resolveSymbolsForDate"
    - path: "backend/src/test/java/com/graphify/trading/rule/definition/RuleDefinitionUniverseTest.java"
      provides: "DATA-03 JSON 역직렬화 테스트"
      exports: ["RuleDefinitionUniverseTest"]
    - path: "backend/src/test/java/com/graphify/market/MarketBarRepositoryTopVolumeTest.java"
      provides: "DATA-04 거래량 상위 N 쿼리 테스트"
      exports: ["MarketBarRepositoryTopVolumeTest"]
  key_links:
    - from: "BacktestService.run()"
      to: "MarketDataPort.topVolumeSymbols()"
      via: "resolveSymbolsForDate() — volume_top_n 분기"
      pattern: "topVolumeSymbols"
    - from: "DbMarketDataAdapter.topVolumeSymbols()"
      to: "MarketBarRepository.findTopVolumeSymbolsOnDate()"
      via: "PageRequest.of(0, topN)"
      pattern: "findTopVolumeSymbolsOnDate"
    - from: "MarketBarRepository.findTopVolumeSymbolsOnDate()"
      to: "Company.inKospi200"
      via: "JOIN Company c ON c.ticker = mb.symbol WHERE c.inKospi200 = TRUE"
      pattern: "inKospi200.*TRUE"
    - from: "RuleDefinitionValidator.validateUniverse()"
      to: "UNIVERSE_TYPES"
      via: "Set.of(\"symbols\", \"watchlist\", \"volume_top_n\")"
      pattern: "volume_top_n"
---

<objective>
volume_top_n 동적 유니버스 타입을 BacktestService 전체 스택에 추가한다.

Purpose: 사용자가 {"type":"volume_top_n","market":"KOSPI","topN":10} 유니버스로 백테스트를 실행하면, 각 날짜마다 그 날의 KOSPI 거래량 상위 10종목을 동적으로 선정해 룰을 평가한다. 이것이 Phase 0의 핵심 목표이며 look-ahead bias 없는 동적 유니버스의 기반이 된다.
Output: 수정된 RuleDefinition.java, RuleDefinitionValidator.java, MarketDataPort.java, MarketBarRepository.java, DbMarketDataAdapter.java, BacktestService.java, 테스트 2개
</objective>

<execution_context>
@/Users/heojeonghun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heojeonghun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-data-infra-dynamic-universe/00-RESEARCH.md
@.planning/phases/00-data-infra-dynamic-universe/00-01-SUMMARY.md
@.planning/phases/00-data-infra-dynamic-universe/00-02-SUMMARY.md

<interfaces>
<!-- 이 플랜에서 변경/추가하는 계약. 탐색 불필요 — 아래를 기준으로 구현하라. -->

From backend/src/main/java/com/graphify/trading/rule/definition/RuleDefinition.java (현재):
```java
@JsonIgnoreProperties(ignoreUnknown = true)
public record Universe(String type, List<String> symbols) {}
// type 허용값: "symbols", "watchlist"
// volume_top_n 필드 없음 — 확장 필요
```

From backend/src/main/java/com/graphify/trading/rule/definition/RuleDefinitionValidator.java (현재):
```java
private static final Set<String> UNIVERSE_TYPES = Set.of("symbols", "watchlist");
// validateUniverse()에서 UNIVERSE_TYPES.contains(u.type()) 체크
// volume_top_n을 추가하고, volume_top_n일 때 symbols 비어있음 체크 건너뜀
```

From backend/src/main/java/com/graphify/trading/engine/MarketDataPort.java (현재):
```java
public interface MarketDataPort {
    List<Bar> historicalDailyBars(String symbol);
    // topVolumeSymbols() 없음 — 추가 필요 (포트 패턴 유지)
}
```

From backend/src/main/java/com/graphify/market/MarketBarRepository.java (현재):
```java
public interface MarketBarRepository extends JpaRepository<MarketBar, Long> {
    List<MarketBar> findBySymbolOrderByTradingDateAsc(String symbol);
    Optional<MarketBar> findBySymbolAndTradingDate(String symbol, LocalDate tradingDate);
    boolean existsBySymbol(String symbol);
    // findTopVolumeSymbolsOnDate() 없음 — 추가 필요
}
```

From backend/src/main/java/com/graphify/trading/backtest/BacktestService.java (00-01 수정 후):
```java
// symbols 결정 현재 구조 (resolveSymbols):
private List<String> resolveSymbols(RuleDefinition def) {
    if (def.universe() == null || def.universe().symbols() == null || def.universe().symbols().isEmpty()) {
        throw new GraphifyException("ERR_BACKTEST_002", "유니버스 종목이 비어 있습니다.", HttpStatus.BAD_REQUEST);
    }
    return def.universe().symbols();
}
// → volume_top_n 타입을 지원하도록 run() 루프 구조 변경 필요
```

**BacktestService 구조 변경 핵심:**
현재: symbols 미리 결정 → 동일 종목으로 모든 날짜 평가
변경: volume_top_n이면 전체 KOSPI 200 종목 데이터 미리 로드 → 날짜 루프 안에서 그 날의 상위 N 종목 선정 → 해당 종목만 평가

**Look-ahead bias 방지:** findTopVolumeSymbolsOnDate(date, topN) 쿼리는 반드시 해당 date의 market_bars 데이터만 사용한다. 미래 데이터 혼입 없음.

**포트 패턴 준수:** BacktestService는 MarketBarRepository를 직접 주입하지 않는다. MarketDataPort에 topVolumeSymbols() 추가 → DbMarketDataAdapter에서 구현.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): 테스트 스텁 작성 — Universe 역직렬화 + 거래량 상위 쿼리</name>
  <files>
    backend/src/test/java/com/graphify/trading/rule/definition/RuleDefinitionUniverseTest.java,
    backend/src/test/java/com/graphify/market/MarketBarRepositoryTopVolumeTest.java
  </files>
  <behavior>
    RuleDefinitionUniverseTest (단위 테스트, ObjectMapper 직접 사용):
    - Test 1: {"type":"volume_top_n","market":"KOSPI","topN":10,"additionalSymbols":["005930"]} → universe.type()="volume_top_n", topN()=10, market()="KOSPI", additionalSymbols()=["005930"]
    - Test 2: 기존 {"type":"symbols","symbols":["005930"]} JSON이 여전히 정상 역직렬화된다
    - Test 3: volume_top_n 타입에서 symbols 필드가 없어도 역직렬화 오류 없음 (@JsonIgnoreProperties)
    - Test 4: additionalSymbols 없는 {"type":"volume_top_n","market":"KOSPI","topN":5} → additionalSymbols()=null

    MarketBarRepositoryTopVolumeTest (@DataJpaTest):
    - Test 1: 5개 종목 데이터(in_kospi200=true) 삽입, 날짜 2024-01-15, volume [100,300,200,400,150] → findTopVolumeSymbolsOnDate(2024-01-15, PageRequest.of(0,3)) = [vol400종목, vol300종목, vol200종목] 순
    - Test 2: in_kospi200=false 종목은 결과에 포함되지 않는다
    - Test 3: volume=null 봉은 결과에 포함되지 않는다
    - Test 4: 다른 날짜의 데이터는 지정 날짜 쿼리에 포함되지 않는다
  </behavior>
  <action>
    두 테스트 파일을 먼저 작성한다. Universe record에 market/topN/additionalSymbols 필드가 없고 Repository에 findTopVolumeSymbolsOnDate()가 없으므로 컴파일 오류로 RED가 된다.

    RuleDefinitionUniverseTest: Jackson ObjectMapper를 직접 생성하거나 @SpringBootTest 없이 사용. RuleDefinition 역직렬화 시나리오를 테스트한다.

    MarketBarRepositoryTopVolumeTest: @DataJpaTest 사용. TestEntityManager로 Company(in_kospi200=true/false)와 MarketBar를 insert 후 쿼리 결과를 검증한다. MarketBar 저장 시 Company도 함께 저장해야 JOIN 쿼리가 동작한다.
  </action>
  <verify>
    <automated>MISSING — Task 1이 테스트 스텁을 생성한다. Task 2 완료 후 GREEN 확인:
    ./gradlew test --tests "com.graphify.trading.rule.definition.RuleDefinitionUniverseTest" --tests "com.graphify.market.MarketBarRepositoryTopVolumeTest" -x integrationTest</automated>
  </verify>
  <done>두 테스트 파일 존재, 컴파일 오류(RED) 상태 — 이후 Task 2에서 GREEN으로 만듦</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Universe 확장 + Repository 쿼리 + 포트 + Validator</name>
  <files>
    backend/src/main/java/com/graphify/trading/rule/definition/RuleDefinition.java,
    backend/src/main/java/com/graphify/trading/rule/definition/RuleDefinitionValidator.java,
    backend/src/main/java/com/graphify/trading/engine/MarketDataPort.java,
    backend/src/main/java/com/graphify/market/MarketBarRepository.java,
    backend/src/main/java/com/graphify/market/DbMarketDataAdapter.java
  </files>
  <behavior>
    Task 1 테스트가 GREEN이 되도록 계약을 구현한다.
  </behavior>
  <action>
    **변경 1 — RuleDefinition.Universe record 확장:**

    현재:
    ```java
    public record Universe(String type, List<String> symbols) {}
    ```

    변경 후:
    ```java
    @JsonIgnoreProperties(ignoreUnknown = true)
    public record Universe(
        String type,                    // "symbols" (기존) | "volume_top_n" (신규) | "watchlist" (기존)
        List<String> symbols,           // type="symbols"일 때 사용
        String market,                  // type="volume_top_n"일 때: "KOSPI"
        Integer topN,                   // type="volume_top_n"일 때: 10
        List<String> additionalSymbols  // volume_top_n + 수동 추가 종목 (optional)
    ) {}
    ```

    기존 JSON {"type":"symbols","symbols":["005930"]}은 @JsonIgnoreProperties(ignoreUnknown=true)로 market/topN/additionalSymbols가 null이 되어 하위 호환 유지.

    **변경 2 — RuleDefinitionValidator.java:**

    UNIVERSE_TYPES에 "volume_top_n" 추가:
    ```java
    private static final Set<String> UNIVERSE_TYPES = Set.of("symbols", "watchlist", "volume_top_n");
    ```

    validateUniverse() 메서드에서 symbols 비어있음 체크를 volume_top_n일 때 건너뛰도록 수정:
    ```java
    private void validateUniverse(Universe u) {
        require(u != null && u.type() != null, "universe.type 이 필요합니다.");
        require(UNIVERSE_TYPES.contains(u.type()), "지원하지 않는 universe.type 입니다.");
        if ("symbols".equals(u.type())) {
            require(u.symbols() != null && !u.symbols().isEmpty(),
                    "universe.symbols 가 비어 있습니다.");
        }
        if ("volume_top_n".equals(u.type())) {
            require(u.topN() != null && u.topN() > 0,
                    "volume_top_n 유니버스는 topN > 0 이 필요합니다.");
        }
        // watchlist 타입은 기존 로직 유지
    }
    ```

    **변경 3 — MarketDataPort.java에 메서드 2개 추가:**

    ```java
    public interface MarketDataPort {
        List<Bar> historicalDailyBars(String symbol);

        /**
         * 지정 날짜에 in_kospi200=true 종목 중 거래량 상위 topN 종목 티커 반환.
         * Look-ahead bias 방지: 해당 날짜의 데이터만 사용.
         */
        default List<String> topVolumeSymbols(LocalDate date, int topN) {
            return List.of();
        }

        /**
         * 지정 시장의 in_kospi200=true 종목 전체 티커 반환.
         * volume_top_n 백테스트 시 데이터 사전 로드에 사용.
         */
        default List<String> symbolsByMarket(String market) {
            return List.of();
        }
    }
    ```

    default 메서드로 추가하면 기존 MarketDataPort 구현체(테스트용 스텁 등)가 깨지지 않는다.

    import 추가:
    ```java
    import java.time.LocalDate;
    import java.util.List;
    ```

    **변경 4 — MarketBarRepository.java에 JPQL 쿼리 2개 추가:**

    ```java
    @Query("""
        SELECT mb.symbol
        FROM MarketBar mb
        JOIN Company c ON c.ticker = mb.symbol
        WHERE mb.tradingDate = :date
          AND c.inKospi200 = TRUE
          AND mb.volume IS NOT NULL
        ORDER BY mb.volume DESC
        """)
    List<String> findTopVolumeSymbolsOnDate(
        @Param("date") LocalDate date,
        Pageable pageable
    );

    @Query("""
        SELECT DISTINCT mb.symbol
        FROM MarketBar mb
        JOIN Company c ON c.ticker = mb.symbol
        WHERE c.inKospi200 = TRUE
          AND c.market = :market
        """)
    List<String> findDistinctKospi200Symbols(@Param("market") String market);
    ```

    import 추가:
    ```java
    import com.graphify.company.Company;
    import org.springframework.data.domain.Pageable;
    import org.springframework.data.jpa.repository.Query;
    import org.springframework.data.repository.query.Param;
    ```

    **변경 5 — DbMarketDataAdapter.java에 메서드 2개 구현:**

    ```java
    @Override
    public List<String> topVolumeSymbols(LocalDate date, int topN) {
        return barRepository.findTopVolumeSymbolsOnDate(date, PageRequest.of(0, topN));
    }

    @Override
    public List<String> symbolsByMarket(String market) {
        return barRepository.findDistinctKospi200Symbols(market);
    }
    ```

    import 추가:
    ```java
    import java.time.LocalDate;
    import java.util.List;
    import org.springframework.data.domain.PageRequest;
    ```
  </action>
  <verify>
    <automated>./gradlew test --tests "com.graphify.trading.rule.definition.RuleDefinitionUniverseTest" --tests "com.graphify.market.MarketBarRepositoryTopVolumeTest" -x integrationTest</automated>
  </verify>
  <done>RuleDefinitionUniverseTest 4개 + MarketBarRepositoryTopVolumeTest 4개 모두 GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BacktestService 동적 유니버스 선정 로직 구현</name>
  <files>
    backend/src/main/java/com/graphify/trading/backtest/BacktestService.java
  </files>
  <behavior>
    volume_top_n 타입 유니버스로 백테스트 실행 시:
    - 먼저 KOSPI 200 전체 종목(additionalSymbols 포함) 데이터를 미리 로드한다
    - 날짜 루프 내에서 해당 날짜의 거래량 상위 N 종목을 marketData.topVolumeSymbols(date, topN)으로 선정한다
    - additionalSymbols가 있으면 동적 선정 결과에 합산한다
    - 선정된 종목 중 closesBySymbol에 데이터가 없는 종목은 건너뛴다
    - 기존 symbols 타입 유니버스는 동작 변경 없음
  </behavior>
  <action>
    BacktestService.run() 메서드를 다음과 같이 재구성한다.

    **핵심 구조 변경:**

    현재 구조:
    ```
    resolveSymbols() → symbols 리스트 고정
    → symbol 루프로 데이터 로드
    → date 루프 → symbol 루프로 평가
    ```

    변경 후 구조:
    ```
    resolveInitialSymbols() → volume_top_n이면 전체 후보 로드, symbols이면 기존
    → symbol 루프로 데이터 로드 (closesBySymbol, volumesBySymbol 등)
    → date 루프
        → resolveSymbolsForDate(def, date, closesBySymbol) → 그날의 유효 종목 결정
        → resolveSymbolsForDate 결과 symbol 루프로 평가
    ```

    **구체적 코드 변경:**

    1. `resolveSymbols()` 메서드를 `resolveInitialSymbols()`로 교체:

    ```java
    /**
     * 데이터 로드용 초기 종목 결정.
     * - symbols 타입: 유니버스 종목 목록 그대로
     * - volume_top_n 타입: 해당 시장의 KOSPI 200 전체 종목 + additionalSymbols
     *   (날짜 루프에서 거래량 상위 N종목만 평가하지만, 데이터는 미리 전체 로드해야 함)
     */
    private List<String> resolveInitialSymbols(RuleDefinition def) {
        Universe u = def.universe();
        if (u == null) throw new GraphifyException(
                "ERR_BACKTEST_002", "유니버스가 없습니다.", HttpStatus.BAD_REQUEST);

        if ("volume_top_n".equals(u.type())) {
            String market = u.market() != null ? u.market() : "KOSPI";
            // KOSPI 200 전체 종목 로드 (날짜별 동적 선정의 후보군)
            Set<String> all = new LinkedHashSet<>(marketData.symbolsByMarket(market));
            // additionalSymbols 합산
            if (u.additionalSymbols() != null) all.addAll(u.additionalSymbols());
            if (all.isEmpty()) throw new GraphifyException(
                    "ERR_BACKTEST_002", "volume_top_n 유니버스에 수집된 종목이 없습니다. "
                    + "먼저 ingestDailyForKospi200()를 실행하세요.", HttpStatus.BAD_REQUEST);
            return new ArrayList<>(all);
        }

        // symbols / watchlist 타입
        if (u.symbols() == null || u.symbols().isEmpty()) throw new GraphifyException(
                "ERR_BACKTEST_002", "유니버스 종목이 비어 있습니다.", HttpStatus.BAD_REQUEST);
        return u.symbols();
    }
    ```

    import 추가 (BacktestService 상단):
    ```java
    import java.util.ArrayList;
    ```
    (LinkedHashSet, Set은 Task 3 마지막 import 섹션에 이미 추가 예정)

    2. 날짜별 유효 종목 결정 메서드 추가:

    ```java
    /**
     * 해당 날짜의 실제 평가 종목 결정.
     * - symbols 타입: closesBySymbol의 모든 종목 (date에 데이터 있는 것)
     * - volume_top_n 타입: marketData.topVolumeSymbols(date, topN) + additionalSymbols
     *   (단, closesBySymbol에 데이터가 있는 종목만)
     */
    private List<String> resolveSymbolsForDate(
            RuleDefinition def, LocalDate date,
            Map<String, double[]> closesBySymbol,
            Map<String, Map<LocalDate, Integer>> indexBySymbol) {

        Universe u = def.universe();
        if ("volume_top_n".equals(u.type())) {
            int topN = u.topN() != null ? u.topN() : 10;
            List<String> dynamic = marketData.topVolumeSymbols(date, topN);
            // LinkedHashSet으로 순서 유지 + 중복 제거
            Set<String> result = new LinkedHashSet<>(dynamic);
            if (u.additionalSymbols() != null) result.addAll(u.additionalSymbols());
            // closesBySymbol에 없는 종목(데이터 없음)은 건너뜀
            return result.stream()
                    .filter(s -> closesBySymbol.containsKey(s)
                            && indexBySymbol.get(s).containsKey(date))
                    .toList();
        }
        // symbols/watchlist 타입: 기존 방식 — closesBySymbol 전체 키
        return closesBySymbol.keySet().stream()
                .filter(s -> indexBySymbol.get(s).containsKey(date))
                .toList();
    }
    ```

    3. run() 메서드 수정:

    - `List<String> symbols = resolveSymbols(def);` → `List<String> symbols = resolveInitialSymbols(def);`
    - 데이터 로드 루프는 기존 유지 (symbols 리스트로 로드)
    - date 루프 내부 `for (String symbol : closesBySymbol.keySet())` →
      `for (String symbol : resolveSymbolsForDate(def, date, closesBySymbol, indexBySymbol))`
    - volume_top_n 타입의 경우 date 루프에서 동적으로 추가된 종목을 closesBySymbol에서 찾을 수 없으면 자동으로 제외됨

    4. import 추가:

    ```java
    import java.util.LinkedHashSet;
    import java.util.Set;
    ```

    (LinkedHashMap은 기존에 이미 있으므로 확인 후 중복 추가 금지)

    **중요 — 빈 유니버스 날짜 처리:**
    `resolveSymbolsForDate`가 빈 리스트를 반환하는 날짜(데이터 없음)는 평가를 건너뛴다. equity point는 이전 날 값으로 유지된다(기존 구조에서 `curve.add(...)` 위치가 날짜 루프 끝에 있으므로 포지션 변화 없이 ledger.equity(lastPrices)를 그대로 기록함 — 변경 불필요).
  </action>
  <verify>
    <automated>./gradlew test --tests "com.graphify.trading.backtest.*" --tests "com.graphify.trading.rule.definition.*" --tests "com.graphify.market.MarketBarRepositoryTopVolumeTest" -x integrationTest</automated>
  </verify>
  <done>
    - 기존 BacktestServiceVolumeTest (00-01에서 작성) GREEN 유지
    - RuleDefinitionUniverseTest GREEN
    - MarketBarRepositoryTopVolumeTest GREEN
    - volume_top_n 유니버스로 BacktestRequest 실행 시 ERR_BACKTEST_002 오류 없음
    - `./gradlew test` 전체 스위트 GREEN
  </done>
</task>

</tasks>

<verification>
Full test suite:
```
./gradlew test --tests "com.graphify.trading.backtest.*" \
               --tests "com.graphify.trading.rule.definition.*" \
               --tests "com.graphify.market.MarketBarRepositoryTopVolumeTest" \
               --tests "com.graphify.trading.engine.*" \
               -x integrationTest
```

Hand-verify (POST to backtest API with volume_top_n universe):
```json
{
  "definition": {
    "version": 1,
    "universe": {"type": "volume_top_n", "market": "KOSPI", "topN": 10},
    "entry": {"logic": "AND", "conditions": [{"left": {"indicator": "RSI", "params": {"period": 14}}, "op": "<", "right": {"value": 30}}]},
    "exit": {"takeProfitPct": 5.0, "stopLossPct": -3.0},
    "sizing": {"type": "cash", "value": 500000}
  },
  "from": "2024-01-01",
  "to": "2024-03-31"
}
```
응답: 200 OK, trades 배열에 거래 기록 (market_bars에 KOSPI 200 데이터가 있는 경우).
</verification>

<success_criteria>
- DATA-03: volume_top_n JSON 역직렬화 — RuleDefinitionUniverseTest 4개 GREEN
- DATA-04: 거래량 상위 N 쿼리 — MarketBarRepositoryTopVolumeTest 4개 GREEN
- DATA-04: BacktestService가 날짜별 동적 유니버스 선정 후 평가 (기존 symbols 타입 회귀 없음)
- RuleDefinitionValidator가 volume_top_n 타입 허용 (ERR_RULE_001 발생 안 함)
- `./gradlew test` 전체 GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/00-data-infra-dynamic-universe/00-04-SUMMARY.md`
</output>
