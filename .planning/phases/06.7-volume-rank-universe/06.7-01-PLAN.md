---
phase: 06.7-volume-rank-universe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/graphify/market/volume/VolumeRankingProvider.java
  - backend/src/main/java/com/graphify/market/volume/DbVolumeRankingAdapter.java
  - backend/src/main/java/com/graphify/company/Company.java
  - backend/src/main/java/com/graphify/market/MarketBarRepository.java
  - backend/src/main/resources/db/migration/V36__instrument_type.sql
  - backend/src/test/java/com/graphify/market/volume/VolumeRankingProviderDbTest.java
autonomous: false
requirements: [DATA-06]

must_haves:
  truths:
    - "백테스트 volume_top_n이 instrument_type='COMMON_STOCK' 종목 중 날짜별 거래량 상위 N을 반환한다 (ETF/ETN/우선주 제외)"
    - "VolumeRankingProvider 포트가 백테스트(DB)·라이브를 통일할 계약으로 존재한다"
    - "companies.instrument_type 컬럼이 마이그레이션·엔티티 양쪽에 존재한다"
    - "KRX getJsonData.cmd 장중 접근 가능 여부가 curl로 확인되어 라이브 어댑터 전략(KRX vs Yahoo fallback)이 결정된다"
  artifacts:
    - path: "backend/src/main/java/com/graphify/market/volume/VolumeRankingProvider.java"
      provides: "topVolume(market, date, topN, excludeEtf) 포트 인터페이스"
      contains: "interface VolumeRankingProvider"
    - path: "backend/src/main/java/com/graphify/market/volume/DbVolumeRankingAdapter.java"
      provides: "백테스트 DB 일봉 랭킹 impl"
      contains: "implements VolumeRankingProvider"
    - path: "backend/src/main/resources/db/migration/V36__instrument_type.sql"
      provides: "companies.instrument_type 컬럼 + COMMON_STOCK 기본값"
      contains: "instrument_type"
    - path: "backend/src/test/java/com/graphify/market/volume/VolumeRankingProviderDbTest.java"
      provides: "DATA-06-SC1 검증"
      contains: "VolumeRankingProviderDbTest"
  key_links:
    - from: "DbVolumeRankingAdapter"
      to: "MarketBarRepository.findTopVolumeByMarketOnDate"
      via: "JPQL instrument_type='COMMON_STOCK' 조건 쿼리"
      pattern: "findTopVolumeByMarketOnDate"
    - from: "MarketBarRepository JPQL"
      to: "Company.instrumentType"
      via: "JOIN Company c ON c.ticker = b.symbol"
      pattern: "instrumentType"
---

<objective>
VolumeRankingProvider 포트 계약을 정의하고 백테스트(DB 일봉) 구현체를 만든다. 동시에 ETF/ETN/우선주 제외를 위한 companies.instrument_type 컬럼을 추가하고, 이 페이즈 최대 리스크인 KRX MDC 장중 접근 가능 여부를 curl 스파이크로 선검증한다.

Purpose: 포트 계약(SC1)과 종목 분류(SC2)의 기반을 먼저 확정해 Plan 02(라이브 KRX 어댑터)·Plan 03(라이브 재선정)이 안정된 인터페이스 위에서 구현되도록 한다. KRX 스파이크는 라이브 어댑터 구현 전략(KRX 직접 vs Yahoo fallback)을 결정하는 게이트다.
Output: VolumeRankingProvider 인터페이스, DbVolumeRankingAdapter, V36 마이그레이션 + Company.instrumentType, 확장 JPQL, VolumeRankingProviderDbTest (DATA-06-SC1).

No UI — backend data-infrastructure phase. (CLAUDE.md Design-in-the-Loop UI/UX 게이트 미적용)
</objective>

<execution_context>
@/Users/heojeonghun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heojeonghun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.7-volume-rank-universe/06.7-CONTEXT.md
@.planning/phases/06.7-volume-rank-universe/06.7-RESEARCH.md
@.planning/phases/06.7-volume-rank-universe/06.7-VALIDATION.md

@backend/src/main/java/com/graphify/trading/engine/MarketDataPort.java
@backend/src/main/java/com/graphify/market/MarketBarRepository.java
@backend/src/main/java/com/graphify/market/DbMarketDataAdapter.java
@backend/src/main/java/com/graphify/company/Company.java

<interfaces>
<!-- 실행자가 그대로 사용. 코드베이스 재탐색 불필요. -->

기존 MarketBarRepository 쿼리 (확장 대상):
```java
// in_kospi200=true 한정 (Pitfall 4 — 전체 KOSPI 보통주로 확장 필요)
@Query("""
    SELECT b.symbol FROM MarketBar b
    JOIN Company c ON c.ticker = b.symbol
    WHERE b.tradingDate = :date AND c.inKospi200 = true AND b.volume IS NOT NULL
    ORDER BY b.volume DESC
    """)
List<String> findTopVolumeSymbolsOnDate(@Param("date") LocalDate date, Pageable pageable);
```

기존 DbMarketDataAdapter (포트 패턴 참고):
```java
@Primary @Component
public class DbMarketDataAdapter implements MarketDataPort {
    public List<String> topVolumeSymbols(LocalDate date, int topN) {
        return barRepository.findTopVolumeSymbolsOnDate(date, PageRequest.of(0, topN));
    }
}
```

Company 엔티티 (instrument_type 추가 대상): @Table(name="companies"), 기존 필드 ticker/market/inKospi200(@Column name="in_kospi200"). getter 패턴 동일하게 추가.

@DataJpaTest 패턴 (RESEARCH Pitfall 6 — Flyway 비활성, H2 create-drop, Entity 기준 스키마 생성):
  - 참고 기존 테스트: backend/src/test/java/com/graphify/market/MarketBarRepositoryTopVolumeTest.java
  - instrument_type은 Company 엔티티에 @Column + 기본값으로 추가해야 H2 자동 생성에 반영됨.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: KRX MDC 장중 접근 스파이크 (Wave 0 선행 검증)</name>
  <what-built>
    실행자가 아래 curl을 실행해 KRX getJsonData.cmd 무인증 접근 가능 여부와 ACC_TRDVOL 갱신 여부를 확인한다. 이 결과가 Plan 02 라이브 어댑터 전략(KRX 직접 vs Yahoo fallback)을 결정한다.
  </what-built>
  <action>
    실행자: 아래 명령을 그대로 실행하고 응답을 SUMMARY에 기록한다.

    ```bash
    curl -s -X POST 'http://data.krx.co.kr/comm/bldAttendant/getJsonData.cmd' \
      -H 'Referer: http://data.krx.co.kr/contents/MDC/MDI/mdiLoader/index.cmd?menuId=MDC0201' \
      -H 'User-Agent: Mozilla/5.0 (compatible; graphify/0.1)' \
      -H 'Content-Type: application/x-www-form-urlencoded' \
      --data 'bld=dbms/MDC/STAT/standard/MDCSTAT01501&mktId=STK&trdDd='"$(date +%Y%m%d)"'&share=1&money=1&csvxls_isNo=true' \
      | head -c 800
    ```

    판정 기준 (RESEARCH §인증 상황):
    - output 배열에 ISU_SRT_CD/ACC_TRDVOL/SECT_TP_NM 포함 → KRX 직접 어댑터 진행 (Plan 02 기본 경로)
    - HTTP 401 또는 output 빈 배열 → 주말/장외이면 trdDd를 직전 거래일로 바꿔 재시도. 그래도 빈/차단이면 Plan 02는 Yahoo 5m 누적 fallback 경로 채택
    - 장중이면 1분 간격 2회 호출해 ACC_TRDVOL 변동 확인 (1분 TTL 캐시 적정성)

    이 태스크는 외부 API 응답 의존이라 자동화 불가. 결과(접근 가능/차단, SECT_TP_NM 필드 존재 여부, 갱신 주기)를 SUMMARY에 명시.
  </action>
  <resume-signal>응답 요약(접근 가능 여부 + SECT_TP_NM 존재 + Plan 02 경로 결정)을 기록 후 "확인" 입력</resume-signal>
</task>

<task type="auto">
  <name>Task 2: instrument_type 마이그레이션 + Company 엔티티 필드</name>
  <files>backend/src/main/resources/db/migration/V36__instrument_type.sql, backend/src/main/java/com/graphify/company/Company.java</files>
  <action>
    V36__instrument_type.sql 생성:
    ```sql
    ALTER TABLE companies ADD COLUMN IF NOT EXISTS instrument_type VARCHAR(20) NOT NULL DEFAULT 'COMMON_STOCK';
    ```
    값 도메인: COMMON_STOCK / ETF / ETN / PREFERRED / SPAC / ETC. 기존 companies 행은 기본값 COMMON_STOCK로 분류 (RESEARCH Open Question 3 — 전체 KOSPI 800 시드는 범위 외, 기존 종목 분류만).

    Company.java에 필드 추가 (RESEARCH Pitfall 6 — @DataJpaTest H2 자동 생성에 반영되도록 엔티티 기본값 포함):
    ```java
    @Column(name = "instrument_type", nullable = false)
    private String instrumentType = "COMMON_STOCK";

    public String getInstrumentType() { return instrumentType; }
    public void setInstrumentType(String instrumentType) { this.instrumentType = instrumentType; }
    ```
    inKospi200 컬럼 선언 패턴을 그대로 따른다. Flyway 마이그레이션 네이밍 컨벤션(V{n}__name.sql, V35가 최신)을 준수.
  </action>
  <verify>
    <automated>cd backend && ./gradlew compileJava -q</automated>
  </verify>
  <done>V36 SQL 파일 존재 + instrument_type 컬럼 정의, Company.instrumentType 필드/getter/setter 추가, compileJava 통과</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: VolumeRankingProvider 포트 + DbVolumeRankingAdapter + 확장 JPQL</name>
  <files>backend/src/main/java/com/graphify/market/volume/VolumeRankingProvider.java, backend/src/main/java/com/graphify/market/volume/DbVolumeRankingAdapter.java, backend/src/main/java/com/graphify/market/MarketBarRepository.java, backend/src/test/java/com/graphify/market/volume/VolumeRankingProviderDbTest.java</files>
  <behavior>
    - Test 1 (DATA-06-SC1): 동일 날짜에 거래량이 다른 보통주 3종목 + ETF 1종목 적재 → topVolume("KOSPI", date, 2, true)가 거래량 상위 2 보통주 티커를 DESC 순으로 반환, ETF 제외
    - Test 2: topN이 가용 종목보다 크면 가용 전체만 반환 (IndexOutOfBounds 없음)
    - Test 3: instrument_type='COMMON_STOCK'이 아닌 종목(ETF/PREFERRED)은 결과에서 제외
  </behavior>
  <action>
    com.graphify.market.volume 패키지 신설.

    VolumeRankingProvider.java (포트 — Plan 02/03이 의존하는 계약):
    ```java
    package com.graphify.market.volume;
    import java.time.LocalDate;
    import java.util.List;
    public interface VolumeRankingProvider {
        /** 거래량 상위 N 티커. 백테스트=완결 일봉 기준 date, 라이브=오늘 당일 누적. */
        List<String> topVolume(String market, LocalDate date, int topN, boolean excludeEtf);
    }
    ```

    MarketBarRepository에 확장 쿼리 추가 (Pitfall 4 — in_kospi200 제약 제거, instrument_type 조건):
    ```java
    @Query("""
        SELECT b.symbol FROM MarketBar b
        JOIN Company c ON c.ticker = b.symbol
        WHERE b.tradingDate = :date AND c.market = :market
          AND c.instrumentType = 'COMMON_STOCK' AND b.volume IS NOT NULL
        ORDER BY b.volume DESC
        """)
    List<String> findTopVolumeByMarketOnDate(@Param("market") String market,
                                             @Param("date") LocalDate date, Pageable pageable);
    ```
    기존 findTopVolumeSymbolsOnDate는 제거하지 말 것 (다른 호출자 보호 — DbMarketDataAdapter.topVolumeSymbols가 사용 중).

    DbVolumeRankingAdapter.java (@Component, NOT @Primary — VolumeRankingProvider 구현체는 백테스트/라이브 2개 공존):
    ```java
    @Component
    public class DbVolumeRankingAdapter implements VolumeRankingProvider {
        private final MarketBarRepository repo;
        // excludeEtf 무시 (쿼리가 이미 COMMON_STOCK 필터). market null이면 "KOSPI" 기본값.
        public List<String> topVolume(String market, LocalDate date, int topN, boolean excludeEtf) {
            String mkt = market != null ? market : "KOSPI";
            return repo.findTopVolumeByMarketOnDate(mkt, date, PageRequest.of(0, topN));
        }
    }
    ```

    VolumeRankingProviderDbTest.java: @DataJpaTest 슬라이스. MarketBarRepositoryTopVolumeTest 패턴 따라 Company + MarketBar 적재(EntityManager 또는 repository.save), DbVolumeRankingAdapter를 직접 인스턴스화(new DbVolumeRankingAdapter(barRepository))하여 topVolume 호출. @DataJpaTest 컨텍스트에 CompanyRepository/MarketBarRepository 주입.
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*VolumeRankingProviderDbTest*"</automated>
  </verify>
  <done>VolumeRankingProvider 인터페이스 + DbVolumeRankingAdapter 존재, findTopVolumeByMarketOnDate 추가(기존 쿼리 보존), VolumeRankingProviderDbTest 3 케이스 green (ETF 제외 + DESC + topN clamp)</done>
</task>

</tasks>

<verification>
- `cd backend && ./gradlew compileJava` 통과 (V36 마이그레이션 + Company 필드 + 신규 포트 컴파일)
- `cd backend && ./gradlew test --tests "*VolumeRankingProviderDbTest*"` green (DATA-06-SC1)
- KRX 스파이크 결과가 SUMMARY에 기록되어 Plan 02 경로 결정됨
</verification>

<success_criteria>
- SC1 부분 충족: VolumeRankingProvider 포트 + 백테스트 DB impl 존재
- SC2 부분 충족: instrument_type 컬럼으로 ETF/ETN/우선주 제외 (COMMON_STOCK 필터)
- KRX 접근 스파이크 완료 → Plan 02 어댑터 전략 결정
</success_criteria>

<output>
After completion, create `.planning/phases/06.7-volume-rank-universe/06.7-01-SUMMARY.md` — KRX 스파이크 결과(접근 가능 여부 + SECT_TP_NM 존재 + 갱신 주기 + Plan 02 권장 경로)를 반드시 포함.
</output>
