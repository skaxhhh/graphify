---
phase: 06.7-volume-rank-universe
plan: 02
type: execute
wave: 2
depends_on: ["06.7-01"]
files_modified:
  - backend/src/main/java/com/graphify/market/volume/KrxMdcVolumeAdapter.java
  - backend/src/test/java/com/graphify/market/volume/KrxMdcVolumeAdapterTest.java
autonomous: true
requirements: [DATA-06]

must_haves:
  truths:
    - "라이브 거래량 랭킹이 KRX MDC getJsonData.cmd JSON 응답에서 SECT_TP_NM='보통주'만 필터해 ACC_TRDVOL DESC 순 티커를 반환한다 (ETF/ETN/우선주/관리종목 제외)"
    - "동일 1분 윈도우 내 두 번째 topVolume 호출은 HTTP 재호출 없이 캐시를 반환한다"
    - "KRX 호출 실패(예외/빈 output)에도 NPE 없이 stale 캐시 또는 빈 리스트를 안전 반환한다"
  artifacts:
    - path: "backend/src/main/java/com/graphify/market/volume/KrxMdcVolumeAdapter.java"
      provides: "라이브 KRX MDC 거래량 랭킹 어댑터 (1분 TTL 캐시)"
      contains: "implements VolumeRankingProvider"
    - path: "backend/src/test/java/com/graphify/market/volume/KrxMdcVolumeAdapterTest.java"
      provides: "DATA-06-SC2/SC3 검증"
      contains: "KrxMdcVolumeAdapterTest"
  key_links:
    - from: "KrxMdcVolumeAdapter"
      to: "data.krx.co.kr/comm/bldAttendant/getJsonData.cmd"
      via: "RestClient POST form-urlencoded (MDCSTAT01501, mktId=STK)"
      pattern: "getJsonData"
    - from: "KrxMdcVolumeAdapter.parseRanking"
      to: "SECT_TP_NM='보통주' 필터"
      via: "JSON output 배열 순회 + ISU_SRT_CD 추출"
      pattern: "SECT_TP_NM|보통주"
---

<objective>
라이브 거래량 랭킹의 KRX MDC 구현체 KrxMdcVolumeAdapter를 만든다. getJsonData.cmd를 RestClient POST로 호출해 당일 누적거래량 순위를 가져오고, SECT_TP_NM='보통주' 필터로 ETF/ETN/우선주를 제외하며, 1분 TTL 수동 캐시로 매 틱 재호출을 방지한다.

Purpose: SC1(라이브 impl)·SC2(보통주만)·SC3(1분 캐시)을 충족하는 라이브 어댑터를 Plan 01이 정의한 VolumeRankingProvider 계약 위에 구현한다. Plan 01 KRX 스파이크 결과가 차단으로 나온 경우, RESEARCH §인증 상황의 fallback 분기(아래 action 참조)를 따른다.
Output: KrxMdcVolumeAdapter (@Component, TTL 캐시, 보통주 필터, 실패 안전), KrxMdcVolumeAdapterTest (DATA-06-SC2, SC3).

No UI — backend data-infrastructure phase.
</objective>

<execution_context>
@/Users/heojeonghun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heojeonghun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.7-volume-rank-universe/06.7-RESEARCH.md
@.planning/phases/06.7-volume-rank-universe/06.7-01-SUMMARY.md

@backend/src/main/java/com/graphify/market/volume/VolumeRankingProvider.java

<interfaces>
<!-- Plan 01에서 생성된 계약. 그대로 구현. -->
```java
public interface VolumeRankingProvider {
    List<String> topVolume(String market, LocalDate date, int topN, boolean excludeEtf);
}
```

프로젝트 RestClient 패턴 참고 (동일 방식):
  - backend/src/main/java/com/graphify/.../YahooFinanceChartClient.java (RestClient.Builder 주입, baseUrl/defaultHeader, retrieve().body(JsonNode.class))
  - 단위 테스트 mock: MockRestServiceServer 또는 RestClient.Builder에 ClientHttpRequestFactory mock — YahooFinanceChartClient 테스트 패턴 따름.

KRX 엔드포인트 (RESEARCH §KRX MDC 호출 패턴, HIGH confidence):
  POST http://data.krx.co.kr/comm/bldAttendant/getJsonData.cmd
  form body: bld=dbms/MDC/STAT/standard/MDCSTAT01501 & mktId=STK & trdDd=YYYYMMDD & csvxls_isNo=true
  응답: {"output":[{"ISU_SRT_CD":"005930","ISU_ABBRV":"삼성전자","SECT_TP_NM":"보통주","ACC_TRDVOL":"12345678","MKT_NM":"KOSPI"}, ...]}
  핵심 필드: ISU_SRT_CD(티커), SECT_TP_NM(종목구분: 보통주/ETF/ETN/우선주/SPAC), ACC_TRDVOL(당일누적거래량, DESC 정렬됨)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KrxMdcVolumeAdapter — KRX 호출 + 보통주 필터 + 파싱</name>
  <files>backend/src/main/java/com/graphify/market/volume/KrxMdcVolumeAdapter.java, backend/src/test/java/com/graphify/market/volume/KrxMdcVolumeAdapterTest.java</files>
  <behavior>
    - Test 1 (DATA-06-SC2): mock 응답 output에 보통주 3 + ETF 1 + 우선주 1 → topVolume("KOSPI", today, 3, true)가 보통주 3 티커만, output 순서(ACC_TRDVOL DESC) 유지 반환
    - Test 2: excludeEtf=true일 때 SECT_TP_NM≠"보통주"(ETF/ETN/우선주/SPAC) 전부 제외
    - Test 3: output 없음/빈 배열/HTTP 예외 → 빈 리스트 반환, 예외 전파 없음 (NPE 없음)
    - Test 4: topN > 보통주 수 → 가용 전체만 (subList IndexOutOfBounds 없음)
  </behavior>
  <action>
    com.graphify.market.volume.KrxMdcVolumeAdapter (@Component, implements VolumeRankingProvider).
    RESEARCH §Code Examples 패턴 그대로:
    - RestClient.Builder 생성자 주입, baseUrl=KRX_JSON_URL, Referer/User-Agent defaultHeader
    - topVolume(): trdDd = date.format(BASIC_ISO_DATE), POST contentType APPLICATION_FORM_URLENCODED, body "bld=dbms%2FMDC%2FSTAT%2Fstandard%2FMDCSTAT01501&mktId=STK&trdDd="+trdDd+"&csvxls_isNo=true", retrieve().body(JsonNode.class)
    - parseRanking(JsonNode, excludeEtf): output 순회, excludeEtf && !"보통주".equals(SECT_TP_NM) → skip, ISU_SRT_CD blank 아니면 추가. KRX는 이미 ACC_TRDVOL DESC 정렬 → 순서 보존.
    - try/catch로 예외 시 log.warn + stale 캐시/빈 리스트 반환 (RESEARCH Pitfall 1)
    - subList(0, Math.min(topN, size)) clamp

    **Plan 01 스파이크가 KRX 차단으로 판정된 경우 (06.7-01-SUMMARY 확인):** RESEARCH §인증 상황 표에 따라 분기.
      - 401/빈 응답이지만 KRX_ID/PW 세션으로 가능 → 세션 쿠키 로그인 경로는 범위 외(주석 TODO)로 두고, 무인증 경로 + fallback 우선.
      - 완전 차단 → 동일 인터페이스로 YahooCumulativeVolumeAdapter를 이 파일 대신 구현 (Yahoo 5m intraday 누적 합산 랭킹, MarketDataPort.recentIntradayBars 활용). 단위 테스트는 동일 계약(SC2/SC3 의미: 보통주 필터는 instrument_type DB 조회로 대체, 캐시 동일)으로 작성. 어느 경로든 @Component VolumeRankingProvider 구현체가 정확히 1개의 "라이브" 빈으로 존재해야 함 (DbVolumeRankingAdapter와 빈 충돌 주의 — 라이브 어댑터에 @Primary 또는 @Qualifier 구분; Plan 03이 명시적으로 주입할 것이므로 둘 다 @Component 유지 + Plan 03에서 List<VolumeRankingProvider> 또는 @Qualifier로 선택).

    빈 선택 충돌 방지: DbVolumeRankingAdapter와 KrxMdcVolumeAdapter 모두 VolumeRankingProvider 구현 → 단일 주입 시 충돌. 라이브 어댑터에 @Qualifier 식별자 부여: 클래스에 명시적 빈 이름(@Component("krxMdcVolumeAdapter")) 부여하여 Plan 03이 @Qualifier("krxMdcVolumeAdapter")로 주입. (백테스트 BacktestService는 이 페이즈에서 기존 MarketDataPort 경로 유지 — 변경 불필요.)
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*KrxMdcVolumeAdapterTest*"</automated>
  </verify>
  <done>KrxMdcVolumeAdapter 존재(@Component 명시 빈 이름), 보통주 필터 + 예외 안전 + topN clamp 구현, KrxMdcVolumeAdapterTest Test1-4 green (DATA-06-SC2). 빌드 시 VolumeRankingProvider 빈 충돌 없음.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 1분 TTL 수동 캐시</name>
  <files>backend/src/main/java/com/graphify/market/volume/KrxMdcVolumeAdapter.java, backend/src/test/java/com/graphify/market/volume/KrxMdcVolumeAdapterTest.java</files>
  <behavior>
    - Test 5 (DATA-06-SC3): topVolume 2회 연속 호출 → RestClient(또는 mock)는 1회만 HTTP 호출, 2번째는 캐시 반환 (verify mock 호출 횟수 = 1)
    - Test 6: 캐시 비어있고 fetch가 빈 결과면 캐시 갱신 안 함 (다음 호출이 재시도)
  </behavior>
  <action>
    RESEARCH §캐시 설계 (Manual TTL — Spring @Cacheable 미사용, IntradayBarCacheService 패턴):
    - volatile List<String> cache = List.of(); volatile Instant cacheTs = Instant.EPOCH; static final Duration CACHE_TTL = Duration.ofMinutes(1);
    - topVolume 진입 시 Instant.now().isBefore(cacheTs.plus(CACHE_TTL)) && !cache.isEmpty() → 캐시 subList 반환
    - fetch 성공(비어있지 않음) 시에만 cache/cacheTs 갱신 (Pitfall 5 — 빈 응답으로 캐시 오염 방지)

    테스트에서 TTL 경계 제어: CACHE_TTL을 생성자/패키지-프라이빗 필드로 노출하거나, 테스트는 "동일 윈도우 2회 호출 → mock 1회"만 검증(시간 경과 불필요). mock RestClient 호출 횟수 검증으로 캐시 hit 입증.
  </action>
  <verify>
    <automated>cd backend && ./gradlew test --tests "*KrxMdcVolumeAdapterTest*"</automated>
  </verify>
  <done>1분 TTL 캐시 동작 — 동일 윈도우 2번째 호출이 HTTP 없이 캐시 반환(Test5 green), 빈 fetch는 캐시 미갱신(Test6 green). DATA-06-SC3 충족.</done>
</task>

</tasks>

<verification>
- `cd backend && ./gradlew test --tests "*KrxMdcVolumeAdapterTest*"` green (DATA-06-SC2, SC3)
- `cd backend && ./gradlew compileJava` — VolumeRankingProvider 구현체 빈 충돌 없음 (라이브 어댑터 @Qualifier 식별)
- (manual) DATA-06-INT: 장중 실제 랭킹 정확성은 verify-work 단계 수동 검증 (VALIDATION.md Manual-Only)
</verification>

<success_criteria>
- SC1 완성: 라이브 impl(KrxMdcVolumeAdapter, ~1분 캐시) 존재
- SC2 완성: SECT_TP_NM='보통주' 필터로 ETF/ETN/우선주/관리종목 제외
- SC3 단위 검증: 1분 TTL 캐시
</success_criteria>

<output>
After completion, create `.planning/phases/06.7-volume-rank-universe/06.7-02-SUMMARY.md` — 채택 경로(KRX 직접 vs Yahoo fallback)와 빈 식별 방식(@Qualifier 이름)을 명시.
</output>
