---
phase: 06.6-candle-chart-visualization
plan: 03
type: execute
wave: 2
depends_on: ["06.6-01", "06.6-02"]
files_modified:
  - frontend/src/components/backtest/CandleSection.tsx
  - frontend/src/pages/trading/paper/PaperBacktestPage.tsx
  - frontend/src/pages/trading/paper/PaperHistoryPage.tsx
  - frontend/e2e/candle-chart.spec.ts
autonomous: false
requirements: [CHART-07]
must_haves:
  truths:
    - "Candle chart section renders below the equity curve on PaperBacktestPage and below the table header on PaperHistoryPage"
    - "On result/history load, the first trade's (symbol, date) auto-loads its candles"
    - "Clicking a trade row loads that (symbol, date)'s candles AND toggles the 6.5 rationale accordion (no conflict)"
    - "Section shows 4 states: empty (no trades / no bars), loading spinner, error banner + retry, success (chart)"
    - "Clicked trade's marker is emphasized and scrolled into view"
  artifacts:
    - path: "frontend/src/components/backtest/CandleSection.tsx"
      provides: "Self-contained section: bars useQuery + 4-state shell + indicator extraction + CandleChart mount"
      min_lines: 70
      contains: "useQuery"
    - path: "frontend/src/pages/trading/paper/PaperBacktestPage.tsx"
      provides: "CandleSection mounted below EquityCurveChart + selectedTradeKey state + row-click wiring"
      contains: "CandleSection"
    - path: "frontend/src/pages/trading/paper/PaperHistoryPage.tsx"
      provides: "CandleSection mounted + row-click wiring (tradedAt-derived date)"
      contains: "CandleSection"
    - path: "frontend/e2e/candle-chart.spec.ts"
      provides: "SC-2..SC-5 Playwright coverage"
      contains: "test"
  key_links:
    - from: "CandleSection useQuery"
      to: "fetchBars(symbol, date)"
      via: "enabled when symbol+date set"
      pattern: "fetchBars"
    - from: "trade row onClick"
      to: "setSelectedTradeKey + setExpandedId"
      via: "single handleRowClick (chart load + accordion toggle)"
      pattern: "setSelectedTradeKey"
    - from: "CandleSection"
      to: "CandleChart"
      via: "bars + markers + indicatorLines props"
      pattern: "<CandleChart"
---

<objective>
Mount the candle chart into both pages: a reusable CandleSection wraps the bars useQuery + 4-state shell +
indicator computation + CandleChart, and is placed below the equity curve (backtest) / above the table (history).
Wire trade-row clicks to select (symbol, date) for the chart while preserving the existing 6.5 rationale accordion.

Purpose: Locked decisions §1 (row-click selection + first-trade auto-load), §6 (4 states, always-expanded section), §9 (same component both pages), §3 (clicked-marker highlight + scroll). Satisfies SC-2..SC-5.
Output: CandleSection.tsx, both pages updated, candle-chart.spec.ts (Wave 0 e2e gap).
</objective>

<execution_context>
@/Users/heojeonghun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heojeonghun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.6-candle-chart-visualization/06.6-RESEARCH.md
@.planning/phases/06.6-candle-chart-visualization/06.6-CONTEXT.md
@CLAUDE.md
@frontend/src/pages/trading/paper/PaperBacktestPage.tsx
@frontend/src/pages/trading/paper/PaperHistoryPage.tsx

<interfaces>
<!-- From Plan 02 (already built) + existing pages. -->

Plan 02 outputs:
- `CandleChart` (components/backtest/CandleChart.tsx): props { bars: CandleBar[]; trades: BacktestTrade[]; filterDate: string; indicatorLines: {label;data}[]; highlightTime?: number }
- `candleIndicators.ts`: extractIndicators(def), computeSMA(bars,period), computeEMA(bars,period), tradesToMarkers(trades,filterDate)
- `fetchBars(symbol, date)` in ruleApi.ts → ApiResponse<CandleBar[]>
- `CandleBar` type in types/trading.ts

Existing page state to integrate with:
- PaperBacktestPage: `result: BacktestResult|null`, `expandedId: number|null`, trades = result.trades (BacktestTrade[] with `datetime`, `symbol`, `companyName`). Rows currently `onClick={() => setExpandedId(...)}`. EquityCurveChart section is the "수익 곡선" card; candle section goes immediately AFTER it.
- PaperHistoryPage: `data: PaperTradeHistoryItem[]`, `expandedId: number|null`, item fields: `id, tradedAt(ISO), symbol, side, qty, price, pnl, rationaleJson` — NO companyName, NO datetime (use `tradedAt`). Candle section goes between the heading and the table (still "always expanded").

Date derivation (RESEARCH Pitfall 8):
- Backtest trade → date = `trade.datetime.slice(0,10)`; symbol = `trade.symbol`.
- History item → date = `trade.tradedAt.slice(0,10)`; symbol = `trade.symbol`.

TradingRule.definition is available on PaperBacktestPage via the rules useQuery (find the selected rule by ruleId to get its definition for extractIndicators). PaperHistoryPage has no rule context → pass `indicators: []` (history shows candles + markers + volume; SMA/EMA overlay is backtest-only, acceptable — history items have no single rule definition in scope).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CandleSection.tsx — bars useQuery + 4-state shell + CandleChart mount</name>
  <files>frontend/src/components/backtest/CandleSection.tsx</files>
  <action>
    Create a self-contained section component reused by both pages. Props:
    ```ts
    interface CandleSectionProps {
      symbol: string | null;      // selected trade's symbol
      date: string | null;        // selected trade's YYYY-MM-DD
      trades: BacktestTrade[];     // ALL trades on the page (for markers; component filters by date)
      indicators: Array<{ indicator: "SMA"|"EMA"; period: number }>; // [] for history page
      highlightTime?: number;      // epoch-seconds of clicked trade
    }
    ```
    Behavior:
    - `useQuery({ queryKey:['backtest','bars',symbol,date], queryFn: async()=> (await fetchBars(symbol!,date!)).data ?? [], enabled: !!symbol && !!date })`.
    - Compute indicatorLines via useMemo from bars + indicators: for each {indicator,period}, data = indicator==='SMA' ? computeSMA(bars,period) : computeEMA(bars,period), label = `${indicator}(${period})`.
    - Section wrapper: always-rendered card `div className="rounded-lg border border-white/10 bg-gray-900/50 p-4"` with heading `<h3 className="mb-4 text-sm font-medium text-gray-300">5분봉 캔들 차트</h3>` (always expanded — locked §6).
    - 4 states INSIDE the card body, all dark-themed inline (RESEARCH Pitfall 7 — do NOT use shared EmptyState/ErrorBanner which are light-theme cream/charcoal). Use these exact dark patterns:
      - No selection (symbol/date null): `<div className="flex h-[400px] items-center justify-center rounded-lg border border-white/10 bg-gray-900/50 text-sm text-gray-500">표시할 거래가 없습니다.</div>`
      - Loading (isLoading): centered spinner — `<div className="flex h-[400px] items-center justify-center"><div className="h-6 w-6 animate-spin rounded-full border-2 border-white/20 border-t-emerald-500" /></div>`.
      - Error (isError): dark error banner + retry button → `<div className="...border-red-500/30 bg-red-500/10 text-red-300...">캔들 데이터를 불러오지 못했습니다. <button onClick={()=>refetch()} className="underline">재시도</button></div>`.
      - Empty bars (success but data.length===0): `<div className="flex h-[400px] ...text-gray-500">해당 일자의 5분봉 데이터가 없습니다.</div>` (distinct copy from no-selection — locked §6 error vs none distinction).
      - Success: `<CandleChart bars={bars} trades={trades} filterDate={date!} indicatorLines={indicatorLines} highlightTime={highlightTime} />`.
    - Add a `data-testid="candle-section"` on the card and `data-testid="candle-chart"` on the success wrapper for e2e.
    Tokens: standard dark palette only; no hardcoded custom hex beyond chart colors already in CandleChart. Per CLAUDE.md, prefer Tailwind utility classes (done above).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>CandleSection renders all 4 states inline-dark, runs bars useQuery, computes indicator lines, mounts CandleChart on success.</done>
</task>

<task type="auto">
  <name>Task 2: Wire CandleSection into PaperBacktestPage + PaperHistoryPage</name>
  <files>frontend/src/pages/trading/paper/PaperBacktestPage.tsx, frontend/src/pages/trading/paper/PaperHistoryPage.tsx</files>
  <action>
    PaperBacktestPage:
    - Add state `const [selected, setSelected] = useState<{ symbol: string; date: string; time: number } | null>(null)`.
    - On mutation onSuccess, after setResult: auto-select first trade — if res.data.trades.length>0, pick trades[0], setSelected({ symbol: t.symbol, date: t.datetime.slice(0,10), time: Math.floor(new Date(t.datetime).getTime()/1000) }); else setSelected(null) (locked §1 first-trade auto-load / 0 trades → null).
    - Replace each row's `onClick` with a single handler: `handleRowClick(t, i)` that does BOTH (RESEARCH Pattern 7): `setSelected({ symbol: t.symbol, date: t.datetime.slice(0,10), time: Math.floor(new Date(t.datetime).getTime()/1000) })` AND the existing accordion toggle (`hasRationale ? setExpandedId(isExpanded?null:i) : undefined`). Keep cursor-pointer/hover on all rows now (clicking always loads chart even without rationale) — make whole row clickable for chart; accordion toggles only when hasRationale.
    - Derive indicators: find selected rule by ruleId in `rules`, `const ruleDef = rules?.find(r=>r.id===ruleId)?.definition; const indicators = ruleDef ? extractIndicators(ruleDef) : []`.
    - Insert `<CandleSection symbol={selected?.symbol ?? null} date={selected?.date ?? null} trades={result.trades} indicators={indicators} highlightTime={selected?.time} />` IMMEDIATELY AFTER the 수익 곡선 card and BEFORE the 고급 통계 section (locked §4 / SC-4 — candle below equity curve).
    - Import CandleSection + extractIndicators.

    PaperHistoryPage:
    - Add `const [selected, setSelected] = useState<{ symbol: string; date: string; time: number } | null>(null)`.
    - On data load, auto-select first item: in a useEffect on `data`, if data && data.length>0 && !selected, setSelected({ symbol: data[0].symbol, date: data[0].tradedAt.slice(0,10), time: Math.floor(new Date(data[0].tradedAt).getTime()/1000) }).
    - Replace row onClick with handler doing BOTH setSelected({ symbol:t.symbol, date:t.tradedAt.slice(0,10), time:Math.floor(new Date(t.tradedAt).getTime()/1000) }) AND existing accordion toggle.
    - Map PaperTradeHistoryItem[] → BacktestTrade[] shape for the section's `trades` prop (markers need `datetime`+`side`+`symbol`): `const candleTrades = data.map(t=>({ datetime: t.tradedAt, symbol: t.symbol, companyName: null, side: t.side, qty: t.qty, price: t.price, pnl: t.pnl, rationaleJson: t.rationaleJson }))`. NOTE tradesToMarkers filters by `datetime.startsWith(filterDate)` — tradedAt is ISO so slice(0,10) prefix matches.
    - Insert `<CandleSection symbol={selected?.symbol ?? null} date={selected?.date ?? null} trades={candleTrades} indicators={[]} highlightTime={selected?.time} />` between the `<h2>` heading and the table block (still inside the data.length>0 branch; when 0 trades the existing empty card already shows).
    - Import CandleSection + useEffect.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>Both pages mount CandleSection in the correct position; first-trade auto-loads; row click loads chart + toggles accordion; backtest passes rule indicators, history passes [].</done>
</task>

<task type="auto">
  <name>Task 3: candle-chart.spec.ts — Playwright e2e (Wave 0 gap, SC-2..SC-5)</name>
  <files>frontend/e2e/candle-chart.spec.ts</files>
  <action>
    Create Playwright spec covering SC-2..SC-5. Follow the existing e2e pattern in frontend/e2e/company-detail-collected-data.spec.ts (read it for auth/baseURL/setup conventions and reuse them). Tests (use the data-testid hooks from Task 1 + a canvas check):
    - `test('renders candlestick', ...)` (SC-2): navigate to PaperBacktestPage, run a backtest (or load a fixture/mock if the suite mocks API like the existing spec), assert `[data-testid="candle-chart"]` becomes visible and contains a `canvas` element.
    - `test('trade markers', ...)` (SC-3): after candles render, assert chart present and at least the section is in success state (marker pixels can't be DOM-asserted; assert canvas exists + no error state — deeper marker visual is a manual-only check per VALIDATION). Keep this assertion DOM-level (canvas present, section not in error/empty).
    - `test('candle below equity', ...)` (SC-4): assert DOM order — the equity curve element ("수익 곡선" card) appears before `[data-testid="candle-section"]` in document order (use locator boundingBox y-comparison or nth ordering).
    - `test('row click loads candles', ...)` (SC-5): click a trade row, assert the chart canvas re-renders / section stays in success state for the clicked row's date (assert `[data-testid="candle-chart"]` visible after click).
    Match whatever API strategy the existing spec uses (real backend vs route mocking). If the existing spec mocks `/api/v1/...` via `page.route`, mock `/api/v1/trading/paper/backtest` (POST) and `/api/v1/trading/paper/backtest/bars` (GET) with small fixtures so tests are deterministic and don't need a live DB.
  </action>
  <verify>
    <automated>cd frontend && npx playwright test --grep "candle" </automated>
  </verify>
  <done>candle-chart.spec.ts green: SC-2 (canvas renders), SC-3 (chart success state), SC-4 (candle below equity DOM order), SC-5 (row click loads candles).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human visual verification — candle section on both pages (markers, overlay, 4 states, placement)</name>
  <action>This is a checkpoint:human-verify task — pause execution and present the &lt;how-to-verify&gt; steps to the user. Do not auto-resume. See &lt;what-built&gt;/&lt;how-to-verify&gt;/&lt;resume-signal&gt; below.</action>
  <what-built>Candle chart section on PaperBacktestPage (below equity curve) and PaperHistoryPage: 5m candlesticks + volume subpanel + buy/sell markers + rule SMA/EMA overlay, with row-click selection, first-trade auto-load, and 4 states (empty/loading/error/none).</what-built>
  <how-to-verify>
    1. `cd frontend && npm run dev`, log in, open the Paper Backtest page.
    2. Run a backtest with a rule that has trades. Confirm: candle chart appears BELOW the 수익 곡선 card and ABOVE 고급 통계 (SC-4); first trade's day auto-loads candles (SC-2).
    3. Confirm markers: BUY = emerald arrowUp below the bar, SELL = red arrowDown above the bar (SC-3). Hover a marker — confirm 6.5 rationale summary shows (manual-only per VALIDATION; e.g. `RSI(14)=28.3 < 30 ✓`).
    4. Confirm SMA/EMA overlay line(s) match the rule's indicators/periods (manual-only — VALIDATION).
    5. Click different trade rows → chart switches to that (symbol, date); clicked marker is emphasized + scrolled into view; the 6.5 accordion still toggles (SC-5).
    6. Force the 4 states: a rule/day with no bars → "해당 일자의 5분봉 데이터가 없습니다." (dark EmptyState); kill backend → ErrorBanner + 재시도; observe loading spinner; 0-trade result → "표시할 거래가 없습니다.". Confirm all dark-themed (no light cream boxes — RESEARCH Pitfall 7) and section is always expanded.
    7. Confirm zoom/pan works on the chart (scroll/drag).
    8. Repeat steps 2-6 (chart + states) on PaperHistoryPage (history rows; no SMA/EMA overlay expected there).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (wrong placement, marker direction/colors, broken states, light-theme leakage, accordion conflict).</resume-signal>
  <verify>Manual visual verification per &lt;how-to-verify&gt; (canvas marker visuals + indicator overlay + 4 dark states are manual-only per VALIDATION.md). Automated coverage is Task 3 (SC-2..SC-5) + `cd frontend && npx playwright test`.</verify>
  <done>User types "approved": markers correct (BUY emerald up / SELL red down), SMA/EMA overlay matches rule, 4 dark states render, candle below equity curve, both pages work, zoom/pan works, accordion coexists.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` clean.
- `cd frontend && npx playwright test --grep "candle"` green (SC-2..SC-5).
- `cd frontend && npx playwright test` full suite green (wave merge gate).
- Visual gate: human-verify checkpoint approved (markers, overlay, 4 states, both pages, zoom/pan).
</verification>

<success_criteria>
- SC-2: 5m candlesticks render (x=time, y=price).
- SC-3: BUY/SELL markers (emerald arrowUp / red arrowDown).
- SC-4: candle section below equity curve on PaperBacktestPage.
- SC-5: row click loads that day's candles; first-trade auto-load on result/history load.
- 4 states all dark-themed; section always expanded; both pages reuse CandleSection/CandleChart.
- 6.5 accordion coexists with chart selection (no conflict).
</success_criteria>

## UI/UX

**Screen inventory (page + states):**
- PaperBacktestPage → candle section placed below 수익 곡선 card, above 고급 통계. States: success (chart with candles/volume/markers/overlay), empty/no-selection ("표시할 거래가 없습니다."), loading (spinner), error (banner + 재시도), no-bars ("해당 일자의 5분봉 데이터가 없습니다.").
- PaperHistoryPage → candle section between heading and trade table (same 5 states; no SMA/EMA overlay).
- Section is ALWAYS expanded (not collapsible) — locked §6.

**shared/ reuse mapping (reuse vs new + justification):**
- New `CandleSection` + (Plan 02) `CandleChart` — justified: no existing candlestick primitive; EquityCurveChart is recharts/static and unsuitable.
- Empty/loading/error states REUSE shared/ `EmptyState`/`ErrorBanner`/`SkeletonBlock` via a new `tone="dark"` variant added to each primitive (default `tone="light"` keeps all existing consumers unchanged). This honors the shared-first rule. Rationale for the dark tone: those primitives previously only had light `cream`/`charcoal` tokens that render as bright boxes inside the dark gray-900 pages (RESEARCH Pitfall 7), so a dark tone was added to the shared primitives rather than forking inline markup. `EmptyState` also gained `showHomeLink={false}` for embedded (in-section) use. (Loading uses `SkeletonBlock` dark instead of a bespoke spinner.)
- Reused as-is: TradeRationaleRow + parseRationale (6.5) for the accordion (unchanged); existing table markup.

**Tokens:** standard Tailwind palette utilities only (gray-900/800, emerald-500/600, red-300/400/500, amber-500, white/10, white/5). No custom cream/charcoal. Chart literal colors documented in CandleChart (Plan 02).

**Responsive / keyboard / focus:**
- Section card is `w-full`; chart `autoSize` → fluid width; fixed 400px height. Trade rows remain keyboard/click reachable; row click drives both chart selection and accordion. Retry button is a focusable `<button>`. Markers' richer info on hover is manual-only (canvas).

<output>
After completion, create `.planning/phases/06.6-candle-chart-visualization/06.6-03-SUMMARY.md`
</output>
