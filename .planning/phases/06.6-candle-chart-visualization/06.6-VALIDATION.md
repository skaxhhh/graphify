---
phase: 6.6
slug: candle-chart-visualization
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-22
---

# Phase 6.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Frontend: Playwright (`@playwright/test ^1.51.0`) + `npm run build` (tsc/vite). Backend: JUnit (Spring Boot `src/test/`) |
| **Config file** | `frontend/playwright.config.ts` (Wave 0: verify exists + baseURL) |
| **Quick run command** | `cd frontend && npm run build` |
| **Full suite command** | `cd frontend && npx playwright test` ; `cd backend && ./mvnw test` |
| **Estimated runtime** | ~60–120 seconds (build + e2e) |

---

## Sampling Rate

- **After every task commit:** Run `cd frontend && npm run build` (catches import/TS errors immediately; backend tasks run `./mvnw -q -Dtest=<class> test`)
- **After every plan wave:** Run `npx playwright test` (frontend) and `./mvnw test` (backend)
- **Before `/gsd:verify-work`:** Full suite green + `npm run build` clean
- **Max feedback latency:** ~120 seconds

---

## Per-Task Verification Map

> Task IDs are filled in by the planner once plans exist. Requirement → test-type mapping below is authoritative.

| Req / SC | Behavior | Wave | Test Type | Automated Command | File Exists | Status |
|----------|----------|------|-----------|-------------------|-------------|--------|
| SC-1 | lightweight-charts installed + CandleChart component exists | 1 | build smoke | `cd frontend && npm run build` | ❌ W0 (CandleChart.tsx) | ⬜ pending |
| SC-2 | 5분봉 캔들스틱 렌더링 (x=시간, y=가격) | — | e2e visual | `npx playwright test --grep "renders candlestick"` | ❌ W0 | ⬜ pending |
| SC-3 | 매수/매도 마커 표시 (arrowUp/arrowDown) | — | e2e | `npx playwright test --grep "trade markers"` | ❌ W0 | ⬜ pending |
| SC-4 | 캔들 차트 수익곡선 아래 배치 | — | e2e DOM order | `npx playwright test --grep "candle below equity"` | ❌ W0 | ⬜ pending |
| SC-5 | 거래일/거래행 선택 → 해당일 캔들 로드 | — | e2e interaction | `npx playwright test --grep "row click loads candles"` | ❌ W0 | ⬜ pending |
| API-1 | bars 엔드포인트가 (symbol,date) OHLCV 리스트 반환 | 1 | backend unit | `cd backend && ./mvnw -Dtest=BarQueryTest test` | ❌ W0 | ⬜ pending |
| API-2 | bars 응답 time 필드 = epoch-seconds (KST 세션) | 1 | backend unit | same as API-1 | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `frontend/e2e/candle-chart.spec.ts` — covers SC-2 through SC-5 (Playwright)
- [ ] `backend/src/test/java/com/graphify/trading/backtest/BarQueryTest.java` — covers API-1, API-2
- [ ] Verify `frontend/playwright.config.ts` exists and `baseURL` is configured (create if missing)
- [ ] Verify/add `getOpen()`, `getHigh()`, `getLow()` getters on `MarketBarIntraday.java` if absent

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| 마커 호버 시 6.5 매매근거 요약 노출 (`RSI(14)=28.3 < 30 ✓`) | CHART-05 | Canvas tooltip content + hover timing hard to assert deterministically in e2e | `browse` PaperBacktestPage, hover a marker, confirm rationale summary matches the trade row's 6.5 accordion |
| 지표선 오버레이(SMA 등) 정확성 | CHART-05 | Visual overlay correctness vs rule definition needs human read | `browse`, confirm overlaid line(s) match rule-defined indicators/periods |
| 4상태 (빈/로딩/에러/없음) 시각 검증 | CHART-04 | Per CLAUDE.md design gate — `browse` + `visual-verdict` against PLAN.md `## UI/UX` inventory | Force each state; confirm shared `EmptyState`/`ErrorBanner`/`SkeletonBlock` used, no token deviations |
| 줌·패닝 인터랙션 | CHART-04 | Canvas gesture interaction | `browse`, scroll/drag chart, confirm zoom/pan works |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
