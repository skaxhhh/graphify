---
phase: 06.5-role-split-trade-rationale
plan: 04
type: execute
wave: 2
depends_on: ["06.5-02"]
files_modified:
  - backend/src/main/java/com/graphify/trading/paper/LiveEvaluationService.java
  - backend/src/main/java/com/graphify/trading/paper/PaperExecutor.java
  - backend/src/main/java/com/graphify/trading/paper/PaperHistoryService.java
  - backend/src/main/java/com/graphify/trading/paper/dto/PaperTradeHistoryItem.java
  - backend/src/test/java/com/graphify/trading/paper/PaperHistoryServiceTest.java
  - backend/src/test/java/com/graphify/trading/paper/PaperExecutorTest.java
autonomous: true
requirements: [RULE-09, MON-05]
must_haves:
  truths:
    - "모의(PAPER_LIVE) 신호 로그의 indicatorSnapshot에 rationale 블록(조건+사유)이 병합 저장된다"
    - "모의 거래 이력 조회가 paper_trades ↔ paper_signal_log JOIN으로 각 거래의 근거 JSON을 함께 반환한다"
    - "상한가/하한가 종목은 PRICE_LIMIT_PENDING으로 감지되어 체결되지 않고 신호 로그에 기록된다"
  artifacts:
    - path: "backend/src/main/java/com/graphify/trading/paper/LiveEvaluationService.java"
      provides: "EvalResult 포착 + indicatorSnapshot에 rationale 병합"
      contains: "evalEntry"
    - path: "backend/src/main/java/com/graphify/trading/paper/dto/PaperTradeHistoryItem.java"
      provides: "rationaleJson 필드"
      contains: "rationaleJson"
    - path: "backend/src/main/java/com/graphify/trading/paper/PaperExecutor.java"
      provides: "PRICE_LIMIT_PENDING 감지 + executed=false 기록"
      contains: "PENDING"
  key_links:
    - from: "PaperHistoryService"
      to: "paper_signal_log"
      via: "rule_id+symbol+ts+signal JOIN으로 rationale 조회"
      pattern: "findByRuleIdAndSymbolAndTs|RuleIdAndSymbol"
    - from: "LiveEvaluationService"
      to: "PaperExecutor.execute"
      via: "rationale 병합된 indicatorSnapshotJson 전달"
      pattern: "indicatorSnapshot|mergeRationale"
---

<objective>
모의(PAPER_LIVE) 경로에 근거 계측을 추가한다: LiveEvaluationService가 EvalResult를 포착해 indicatorSnapshot에 rationale을 병합 저장하고, PaperHistoryService가 paper_trades ↔ paper_signal_log JOIN으로 각 거래의 근거를 함께 반환한다. 추가로 MON-05(상한가/하한가 PRICE_LIMIT_PENDING)를 PaperExecutor에 구현한다.

Purpose: 모의 거래 이력 각 행에 백테스트와 동일한 근거를 제공한다 (RULE-09 SC5). MON-05는 체결 불가 상태를 신호 로그로 가시화한다.
Output: rationale 병합 LiveEvaluationService, JOIN 기반 PaperHistoryService, rationaleJson 필드, PRICE_LIMIT_PENDING 감지, 조인/감지 테스트.
</objective>

<execution_context>
@/Users/heojeonghun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heojeonghun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.5-role-split-trade-rationale/06.5-RESEARCH.md
@.planning/phases/06.5-role-split-trade-rationale/06.5-02-SUMMARY.md

@backend/src/main/java/com/graphify/trading/paper/LiveEvaluationService.java
@backend/src/main/java/com/graphify/trading/paper/PaperExecutor.java
@backend/src/main/java/com/graphify/trading/paper/PaperHistoryService.java
@backend/src/main/java/com/graphify/trading/paper/PaperSignalLog.java

<interfaces>
<!-- Extracted from codebase. EvalResult from Plan 02 (see 06.5-02-SUMMARY.md). -->

PaperExecutor.execute (already takes indicatorSnapshotJson; saveSignalLog records HOLD/BUY/SELL):
```java
public TradeResult execute(Signal signal, TradingRule rule, String symbol, ..., String indicatorSnapshotJson, Instant ts)
private void saveSignalLog(Long ruleId, String symbol, Instant ts, String signal, String indicatorJson, boolean executed)
// signal stored as VARCHAR(8) → "PENDING" (7 chars) fits.
```

LiveEvaluationService decision point (RESEARCH E3, ~line 163-185):
  buildIndicatorSnapshot(closes, last, price) → {price, rsi14, sma20}
  posOpt present → exit path; else entry path.
Merge target JSON (RESEARCH Discretion 2): add "rationale":{side,exitReason,exitPct,conditions[]} into snapshot.

PaperHistoryService.getHistory (currently no rationale):
  tradeRepo.findByAccountIdOrderByTradedAtDesc(accountId).map(t -> new PaperTradeHistoryItem(...))
JOIN key (RESEARCH Discretion 5 + Pitfall 2): rule_id + symbol + ts(=traded_at) + signal(=side).

Current PaperTradeHistoryItem record fields: (id, tradedAt, symbol, side, qty, price, fee, pnl) — add rationaleJson.

MON-05 (Pitfall 7 + Open Q3): detect |price - prevClose|/prevClose >= 0.295 (KRX ±29.5%); prevClose from market_bars 일봉.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LiveEvaluationService EvalResult 포착 + indicatorSnapshot rationale 병합</name>
  <files>backend/src/main/java/com/graphify/trading/paper/LiveEvaluationService.java</files>
  <action>
    RESEARCH E3 패턴:
    1. 신호 결정 지점(~line 163-185)에서 entryTriggered/exitTriggered를 evalEntry/evalExit로 교체해 EvalResult 포착.
    2. private String mergeRationale(String snapshotJson, EvalResult r, String side) 추가: 기존 buildIndicatorSnapshot 결과 JSON에 rationale 블록({side,exitReason,exitPct,conditions[]})을 병합 (ObjectMapper ObjectNode put). Plan 03 buildRationale과 동일 스키마 — 가능하면 공통 헬퍼/유틸로 추출(중복 방지).
    3. 신호가 BUY/SELL일 때만 rationale 병합한 JSON을 PaperExecutor.execute(...)의 indicatorSnapshotJson으로 전달. HOLD는 기존대로.
    4. ObjectMapper는 기존 주입 빈 재사용.
  </action>
  <verify>
    <automated>./gradlew :backend:compileJava</automated>
  </verify>
  <done>LiveEvaluationService가 evalEntry/evalExit 포착, BUY/SELL 신호에 rationale 병합된 indicatorSnapshot 전달. compileJava 성공.</done>
</task>

<task type="auto">
  <name>Task 2: PaperHistoryService JOIN + PaperTradeHistoryItem rationaleJson + 조인 테스트</name>
  <files>backend/src/main/java/com/graphify/trading/paper/PaperHistoryService.java, backend/src/main/java/com/graphify/trading/paper/dto/PaperTradeHistoryItem.java, backend/src/test/java/com/graphify/trading/paper/PaperHistoryServiceTest.java</files>
  <action>
    RESEARCH Discretion 5 + Pitfall 2:
    1. PaperTradeHistoryItem record에 `String rationaleJson` 필드 추가.
    2. PaperHistoryService.getHistory: 각 trade에 대해 signalLogRepo로 rule_id+symbol+ts(=traded_at)+signal(=side) 매칭 로그를 조회해 indicator_snapshot을 rationaleJson으로 부착. 로그 없으면 null. signal=side 조건 포함(동일 ts BUY+SELL 방어). 필요 시 PaperSignalLogRepository에 findByRuleIdAndSymbolAndTsAndSignal 쿼리 메서드 추가.
    3. PaperHistoryServiceTest (Wave 0 스캐폴드): trade + 매칭되는 signal_log(snapshot에 rationale 포함)를 stub하고, getHistory 결과의 rationaleJson이 해당 snapshot과 일치함을 단언. 매칭 로그 없을 때 null 단언.
    NOTE: 비정규화 저장 아님 — JOIN 방식 (RESEARCH 결론).
  </action>
  <verify>
    <automated>./gradlew :backend:test --tests "*.PaperHistoryServiceTest"</automated>
  </verify>
  <done>PaperTradeHistoryItem에 rationaleJson. getHistory가 JOIN으로 근거 부착. 테스트 green.</done>
</task>

<task type="auto">
  <name>Task 3: MON-05 PRICE_LIMIT_PENDING 감지 + PaperExecutorTest 케이스</name>
  <files>backend/src/main/java/com/graphify/trading/paper/PaperExecutor.java, backend/src/test/java/com/graphify/trading/paper/PaperExecutorTest.java</files>
  <action>
    RESEARCH Pitfall 7 + Open Q3:
    1. PaperExecutor.execute에서 BUY/SELL 체결 실행 전, 가격 변동률 체크: |price - prevClose|/prevClose >= 0.295 (KRX ±29.5%)이면 체결하지 않고 saveSignalLog에 signal="PENDING"(또는 기존 signal + executed=false) 기록 후 미체결 TradeResult 반환. prevClose는 전일 일봉 종가(market_bars)에서 조회 — prevClose를 execute 파라미터로 주입하거나 주입된 MarketDataPort로 조회(기존 의존성 패턴 따름).
    2. signal VARCHAR(8) 제약 내 "PENDING"(7자) 사용 가능.
    3. PaperExecutorTest (파일 존재, 케이스 추가): 가격이 prevClose 대비 +30% 같은 상한가 상황에서 execute 호출 시 → executed=false, 포지션 변동 없음, signal log에 PENDING 기록됨을 단언. 정상 변동률에서는 기존대로 체결됨도 확인.
  </action>
  <verify>
    <automated>./gradlew :backend:test --tests "*.PaperExecutorTest"</automated>
  </verify>
  <done>상한가/하한가 종목이 미체결 + 신호 로그 기록됨. 정상 종목은 체결 유지. green.</done>
</task>

</tasks>

<verification>
- `./gradlew :backend:test --tests "com.graphify.trading.*" -x integrationTest` green
- 모의 거래 이력이 JOIN으로 근거 반환, 백테스트와 동일 JSON 스키마
- MON-05 상하한가 감지로 미체결 처리
</verification>

<success_criteria>
- 모의(PAPER_LIVE) 거래 이력 각 행에 근거(조건+값+✓/✗, 청산 사유) 제공 (SC5)
- 백테스트(Plan 03)와 동일 rationale 스키마 → 프론트 단일 컴포넌트 재사용 가능
- 상한가/하한가 PRICE_LIMIT_PENDING 감지 (MON-05)
</success_criteria>

<output>
완료 후 `.planning/phases/06.5-role-split-trade-rationale/06.5-04-SUMMARY.md` 생성. PaperTradeHistoryItem.rationaleJson 필드명과 JOIN 동작을 Plan 05 프론트가 참조하도록 기록.
</output>
