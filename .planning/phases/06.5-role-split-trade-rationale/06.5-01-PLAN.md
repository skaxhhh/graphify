---
phase: 06.5-role-split-trade-rationale
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/resources/db/migration/V35__two_axis_status.sql
  - backend/src/main/java/com/graphify/trading/rule/TradingRule.java
  - backend/src/main/java/com/graphify/trading/rule/RuleStatus.java
  - backend/src/main/java/com/graphify/trading/rule/dto/RuleResponse.java
  - backend/src/main/java/com/graphify/trading/rule/PaperRuleService.java
  - backend/src/main/java/com/graphify/trading/paper/PaperLifecycleService.java
  - backend/src/main/java/com/graphify/trading/paper/PaperLifecycleController.java
  - backend/src/test/java/com/graphify/trading/rule/PaperLifecycleServiceTest.java
  - backend/src/test/java/com/graphify/trading/rule/PaperRuleServiceTest.java
autonomous: true
requirements: [RULE-08]
must_haves:
  truths:
    - "DRAFT 룰을 조건 없이 (백테스트 없이) ACTIVE로 전환할 수 있다"
    - "ACTIVE/STOPPED 룰을 start하면 RUNNING이 되고, stop하면 STOPPED로 복귀한다"
    - "RUNNING 상태 룰은 편집과 ACTIVE→DRAFT 하향이 차단된다"
    - "전략 운영 조회는 config_status=ACTIVE 룰만 반환한다"
    - "스케줄러/평가 게이트(isLiveActive)가 run_status=RUNNING 기준으로 동작한다"
  artifacts:
    - path: "backend/src/main/resources/db/migration/V35__two_axis_status.sql"
      provides: "config_status/run_status 컬럼 + 기존 행 매핑"
      contains: "config_status"
    - path: "backend/src/main/java/com/graphify/trading/rule/TradingRule.java"
      provides: "configStatus/runStatus 필드 + getter/setter"
      contains: "runStatus"
    - path: "backend/src/main/java/com/graphify/trading/paper/PaperLifecycleService.java"
      provides: "activate/deactivate/start/stop 2축 전이 메서드"
      contains: "activate"
    - path: "backend/src/test/java/com/graphify/trading/rule/PaperLifecycleServiceTest.java"
      provides: "전이 가드 + ACTIVE 필터 검증"
      contains: "PaperLifecycleServiceTest"
  key_links:
    - from: "RuleStatus.isLiveActive"
      to: "TradingRule.runStatus"
      via: "run_status=RUNNING 체크"
      pattern: "RUNNING.*getRunStatus|getRunStatus.*RUNNING"
    - from: "PaperLifecycleController"
      to: "PaperLifecycleService.activate/start/stop"
      via: "POST 엔드포인트 매핑"
      pattern: "activate|/start|/stop"
---

<objective>
trading_rules의 단일 `status` 문자열을 2축 상태 모델(`config_status` DRAFT|ACTIVE × `run_status` STOPPED|RUNNING)로 재구성하고, PaperLifecycleService의 promote/pause/resume를 activate/deactivate/start/stop 의미로 교체한다. RULE-01 백테스트 게이트를 폐지하고, RUNNING 중 편집/하향을 차단한다.

Purpose: 모의 룰 "설정"(config축)과 "운영"(run축) 제어를 DB 레벨에서 분리해 두 화면의 역할 경계를 명확히 한다 (RULE-08의 백엔드 토대). Wave 2/3 모든 작업이 이 상태 모델 위에서 동작한다.
Output: V35 마이그레이션, 2축 필드를 가진 TradingRule, run_status 기준 isLiveActive, 새 lifecycle 엔드포인트, 전이 가드 테스트.
</objective>

<execution_context>
@/Users/heojeonghun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heojeonghun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.5-role-split-trade-rationale/06.5-CONTEXT.md
@.planning/phases/06.5-role-split-trade-rationale/06.5-RESEARCH.md
@.planning/phases/06.5-role-split-trade-rationale/06.5-VALIDATION.md

@backend/src/main/java/com/graphify/trading/rule/TradingRule.java
@backend/src/main/java/com/graphify/trading/rule/RuleStatus.java
@backend/src/main/java/com/graphify/trading/paper/PaperLifecycleService.java
@backend/src/main/java/com/graphify/trading/paper/PaperLifecycleController.java
@backend/src/main/java/com/graphify/trading/rule/PaperRuleService.java

<interfaces>
<!-- Extracted from codebase — use directly, no exploration needed. -->

Current RuleStatus.java (single edit point for live-loop gate):
```java
public final class RuleStatus {
    public static final String PAPER_LIVE = "PAPER_LIVE";
    public static boolean isLiveActive(String status) { ... }  // currently PAPER_LIVE.equals(status)
}
```

Current RuleResponse.java (returned to frontend — MUST add configStatus/runStatus):
```java
public record RuleResponse(
    Long id, String name, String mode, String status, boolean backtested,
    JsonNode definition, Long promotedFrom, Instant createdAt, Instant updatedAt
) {}
```

Current PaperLifecycleController endpoints (RequestMapping "/api/v1/trading/paper/rules"):
  POST /{id}/promote  → promote(userId, id)
  POST /{id}/pause    → pause(userId, id)
  POST /{id}/resume   → resume(userId, id)
  POST /{id}/copy     → copy(userId, id)   (keep unchanged)

Latest migration on disk: V34__toss_credentials.sql → new migration is V35.

Existing row mapping (apply in V35 UPDATE):
  PAPER_LIVE          → (config=ACTIVE, run=RUNNING)
  PAUSED              → (config=ACTIVE, run=STOPPED)
  DRAFT / BACKTESTED  → (config=DRAFT,  run=STOPPED)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: V35 마이그레이션 + TradingRule 2축 필드 + RuleStatus 게이트 전환</name>
  <files>backend/src/main/resources/db/migration/V35__two_axis_status.sql, backend/src/main/java/com/graphify/trading/rule/TradingRule.java, backend/src/main/java/com/graphify/trading/rule/RuleStatus.java</files>
  <action>
    1. V35__two_axis_status.sql 생성 (RESEARCH E1 패턴 그대로):
       - ALTER TABLE trading_rules ADD COLUMN IF NOT EXISTS config_status VARCHAR(8) NOT NULL DEFAULT 'DRAFT', ADD COLUMN IF NOT EXISTS run_status VARCHAR(8) NOT NULL DEFAULT 'STOPPED';
       - UPDATE 3건: status='PAPER_LIVE'→(ACTIVE,RUNNING); status='PAUSED'→(ACTIVE,STOPPED); status IN('DRAFT','BACKTESTED')→(DRAFT,STOPPED);
       - CREATE INDEX IF NOT EXISTS idx_trading_rules_run_status ON trading_rules (run_status);
       - 기존 `status` 컬럼은 호환성을 위해 유지(제거하지 않음 — Pitfall 1).
    2. TradingRule.java에 필드 추가 (기존 status 필드는 유지):
       - @Column(name="config_status", nullable=false) private String configStatus = "DRAFT";
       - @Column(name="run_status", nullable=false) private String runStatus = "STOPPED";
       - getter/setter 추가 (기존 코드 스타일과 일치).
    3. RuleStatus.java: run_status=RUNNING 기준 오버로드 추가. 기존 isLiveActive(String status) 시그니처는 보존하고, 새 isLiveActive(TradingRule rule)를 추가해 "RUNNING".equals(rule.getRunStatus()) 반환. RUNNING 상수("RUNNING")와 ACTIVE("ACTIVE") 상수도 정의. 단일 편집 지점 주석 유지.
    NOTE: status 컬럼 완전 제거 금지(런타임 전이 가드 파손, Pitfall 1).
  </action>
  <verify>
    <automated>./gradlew :backend:compileJava</automated>
  </verify>
  <done>V35 파일 존재, config_status/run_status 컬럼 ADD + 3건 UPDATE + 인덱스 포함. TradingRule에 configStatus/runStatus 필드 + getter/setter. RuleStatus.isLiveActive(TradingRule)가 run_status=RUNNING 기준. compileJava 성공.</done>
</task>

<task type="auto">
  <name>Task 2: PaperLifecycleService 2축 재구성 + 컨트롤러 엔드포인트 + RuleResponse 확장 + CRUD 가드</name>
  <files>backend/src/main/java/com/graphify/trading/paper/PaperLifecycleService.java, backend/src/main/java/com/graphify/trading/paper/PaperLifecycleController.java, backend/src/main/java/com/graphify/trading/rule/dto/RuleResponse.java, backend/src/main/java/com/graphify/trading/rule/PaperRuleService.java</files>
  <action>
    RESEARCH Discretion 6 매핑표 그대로 구현:
    1. PaperLifecycleService에 메서드 추가 (기존 promote/pause/resume는 일시 보존 가능하나 새 메서드로 위임 권장):
       - activate(userId, id): configStatus DRAFT→ACTIVE. 백테스트 게이트(!rule.isBacktested()) 제거 — 조건 없이 전환. config만 변경.
       - deactivate(userId, id): configStatus ACTIVE→DRAFT. 가드: runStatus=='RUNNING'이면 throw(먼저 중지 필요).
       - start(userId, id): 가드 configStatus=='ACTIVE' 필수, runStatus STOPPED→RUNNING. 기존 resume의 paperLiveSymbols 재할당 로직 재사용.
       - stop(userId, id): runStatus RUNNING→STOPPED. config는 ACTIVE 유지(PAUSED 상태로 빠지지 않음).
       - copy(): 변경 없음.
       - 모든 getStatus() 기반 가드를 configStatus/runStatus 기반으로 교체 (Pitfall 1).
       - toResponse()에서 configStatus/runStatus를 RuleResponse에 채워 반환.
    2. PaperLifecycleController: 새 엔드포인트 5개 매핑 (기존 promote/pause/resume는 deprecated로 남기되 신규 추가):
       - POST /{id}/activate, /{id}/deactivate, /{id}/start, /{id}/stop, /{id}/copy(기존)
    3. RuleResponse record에 String configStatus, String runStatus 필드 추가 (Pitfall 6). 모든 RuleResponse 생성 지점 업데이트.
    4. PaperRuleService: CRUD 생성/수정 시 config_status만 설정 가능하게 제한, run_status는 lifecycle에서만 변경 (Pitfall 5). normalizeStatus/STATUSES 화이트리스트를 configStatus 기준으로 정비. 룰 정의 수정(update) 시 runStatus=='RUNNING'이면 차단 throw.
  </action>
  <verify>
    <automated>./gradlew :backend:compileJava</automated>
  </verify>
  <done>activate/deactivate/start/stop 메서드 + 4개 신규 엔드포인트 존재. RuleResponse에 configStatus/runStatus 포함. RUNNING 중 update/deactivate가 예외 throw. 백테스트 게이트 제거됨. compileJava 성공.</done>
</task>

<task type="auto">
  <name>Task 3: 전이 가드 + ACTIVE 필터 테스트 작성 (Wave 0 스캐폴드)</name>
  <files>backend/src/test/java/com/graphify/trading/rule/PaperLifecycleServiceTest.java, backend/src/test/java/com/graphify/trading/rule/PaperRuleServiceTest.java</files>
  <action>
    VALIDATION.md Wave 0 스캐폴드 충족. JUnit5 + Mockito 슬라이스 패턴(기존 trading 테스트 참조).
    PaperLifecycleServiceTest:
      - activate: DRAFT 룰 → configStatus ACTIVE, 백테스트 안 했어도 성공.
      - start: ACTIVE/STOPPED → RUNNING. stop: RUNNING → STOPPED (config ACTIVE 유지).
      - deactivate 가드: runStatus=RUNNING이면 예외.
      - 전략 운영 조회: configStatus=ACTIVE 룰만 반환 (DRAFT 제외) — lifecycle 목록 메서드 검증.
    PaperRuleServiceTest:
      - RUNNING 룰 정의 update 시 예외 throw (편집 차단).
      - CRUD가 run_status를 임의 변경하지 못함.
    Mockito로 repository stub, 외부 의존성(스케줄러/심볼 할당) mock.
  </action>
  <verify>
    <automated>./gradlew :backend:test --tests "*.PaperLifecycleServiceTest" --tests "*.PaperRuleServiceTest"</automated>
  </verify>
  <done>두 테스트 파일 존재, 위 케이스 전부 green. RULE-08 전이 가드 + ACTIVE 필터 자동 검증됨.</done>
</task>

</tasks>

<verification>
- `./gradlew :backend:test --tests "com.graphify.trading.*" -x integrationTest` green
- V35 마이그레이션이 Flyway 순서상 V34 다음에 위치, status 컬럼 보존
- isLiveActive가 run_status=RUNNING 기준 (스케줄러/LiveEvaluationService 게이트 일원화)
</verification>

<success_criteria>
- DRAFT→ACTIVE 백테스트 없이 전환 가능 (RULE-01 게이트 폐지)
- start/stop이 run_status만 토글, RUNNING 복귀 시 PAUSED 아님
- RUNNING 중 편집/하향 차단
- RuleResponse가 configStatus/runStatus 노출 (프론트 소비 준비)
- 전략 운영 조회가 ACTIVE만 반환
</success_criteria>

<output>
완료 후 `.planning/phases/06.5-role-split-trade-rationale/06.5-01-SUMMARY.md` 생성. 특히 신규 엔드포인트 경로와 RuleResponse 필드명을 Plan 05(프론트)가 참조하도록 기록.
</output>
