---
phase: 06.5-role-split-trade-rationale
plan: 03
type: execute
wave: 2
depends_on: ["06.5-02"]
files_modified:
  - backend/src/main/java/com/graphify/trading/engine/PaperLedger.java
  - backend/src/main/java/com/graphify/trading/backtest/IntradayBacktestEngine.java
  - backend/src/main/java/com/graphify/trading/backtest/dto/BacktestResult.java
  - backend/src/test/java/com/graphify/trading/backtest/BacktestServiceIntradayTest.java
autonomous: true
requirements: [RULE-09]
must_haves:
  truths:
    - "백테스트 각 거래(TradeDto)에 그 매수/매도가 발생한 트리거 근거 JSON이 인라인으로 포함된다"
    - "근거 JSON은 모의와 동일한 스키마(side, exitReason, exitPct, conditions[])를 따른다"
  artifacts:
    - path: "backend/src/main/java/com/graphify/trading/engine/PaperLedger.java"
      provides: "TradeRecord.rationaleJson + buy/sell rationale 파라미터"
      contains: "rationaleJson"
    - path: "backend/src/main/java/com/graphify/trading/backtest/dto/BacktestResult.java"
      provides: "TradeDto.rationaleJson 필드"
      contains: "rationaleJson"
    - path: "backend/src/main/java/com/graphify/trading/backtest/IntradayBacktestEngine.java"
      provides: "evalEntry/evalExit 포착 + buildRationale + ledger 전달"
      contains: "evalEntry"
  key_links:
    - from: "IntradayBacktestEngine"
      to: "PaperLedger.buy/sell"
      via: "rationaleJson 인자 전달"
      pattern: "buy\\(.*rationale|sell\\(.*rationale"
    - from: "IntradayBacktestEngine"
      to: "RuleEvaluator.evalEntry/evalExit"
      via: "EvalResult 포착"
      pattern: "evalEntry|evalExit"
---

<objective>
백테스트 엔진이 진입/청산 결정 지점에서 EvalResult를 포착해 근거 JSON을 생성하고, in-memory PaperLedger의 TradeRecord와 BacktestResult.TradeDto에 인라인으로 운반한다. 백테스트는 DB 조인이 없으므로 같은 객체에 인라인으로 들고 다니는 방식(RESEARCH Discretion 4)을 사용한다.

Purpose: 백테스트 거래 이력 각 행에 "왜 이 거래가 일어났나" 근거를 제공한다 (RULE-09, SC4). Plan 04(모의)와 동일한 rationale JSON 스키마를 생성해 Plan 05 프론트가 단일 컴포넌트로 양쪽을 렌더링한다.
Output: rationaleJson을 운반하는 PaperLedger/TradeDto, buildRationale 헬퍼, 근거 포함 백테스트 테스트.
</objective>

<execution_context>
@/Users/heojeonghun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heojeonghun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.5-role-split-trade-rationale/06.5-RESEARCH.md
@.planning/phases/06.5-role-split-trade-rationale/06.5-02-SUMMARY.md

@backend/src/main/java/com/graphify/trading/engine/PaperLedger.java
@backend/src/main/java/com/graphify/trading/backtest/IntradayBacktestEngine.java
@backend/src/main/java/com/graphify/trading/backtest/dto/BacktestResult.java

<interfaces>
<!-- Extracted from codebase. EvalResult comes from Plan 02 (see 06.5-02-SUMMARY.md). -->

Current PaperLedger.TradeRecord (add rationaleJson at end):
```java
public record TradeRecord(
    LocalDate date, String symbol, String side, double qty, double price, Double pnl
) {}
// buy(...) / sell(...) gain a trailing String rationaleJson param
```

Current BacktestResult.TradeDto (add rationaleJson at end):
```java
public record TradeDto(
    LocalDateTime datetime, String symbol, String companyName,
    String side, double qty, double price, Double pnl
) {}
```

IntradayBacktestEngine decision point (RESEARCH E2, ~line 123-136):
  holds(symbol) → evalExit(def.exit(), closes, volumes, i, entryPrice)
  else          → evalEntry(def.entry(), closes, volumes, i)
buildResult() maps TradeRecord → TradeDto (~line 241): t.date().atStartOfDay() used for datetime — rationale is index/object-local so no ts matching needed (Pitfall 4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PaperLedger + BacktestResult.TradeDto에 rationaleJson 운반 추가</name>
  <files>backend/src/main/java/com/graphify/trading/engine/PaperLedger.java, backend/src/main/java/com/graphify/trading/backtest/dto/BacktestResult.java</files>
  <action>
    1. PaperLedger.TradeRecord에 `String rationaleJson` 필드를 마지막에 추가.
    2. buy(...)/sell(...) 메서드 시그니처에 `String rationaleJson` 파라미터를 마지막에 추가하고, 생성하는 TradeRecord에 전달.
    3. PaperLedger 내부에서 buy/sell을 호출하는 다른 경로가 있으면 null 또는 적절한 값 전달로 컴파일 유지.
    4. BacktestResult.TradeDto에 `String rationaleJson` 필드를 마지막에 추가.
    NOTE: backtest는 in-memory이므로 인라인 운반(Discretion 4). DB 변경 없음.
  </action>
  <verify>
    <automated>./gradlew :backend:compileJava</automated>
  </verify>
  <done>TradeRecord/TradeDto에 rationaleJson 필드. buy/sell이 rationaleJson 파라미터 수신. compileJava 성공.</done>
</task>

<task type="auto">
  <name>Task 2: IntradayBacktestEngine에서 EvalResult 포착 + buildRationale + 거래에 부착</name>
  <files>backend/src/main/java/com/graphify/trading/backtest/IntradayBacktestEngine.java</files>
  <action>
    RESEARCH E2 패턴:
    1. 진입/청산 결정 지점(~line 123-136)에서 entryTriggered/exitTriggered 호출을 evalEntry/evalExit로 교체해 EvalResult를 포착.
    2. private String buildRationale(EvalResult r, String side) 헬퍼 추가: ObjectMapper로 RESEARCH Discretion 2 스키마의 JSON 문자열 생성 — {side, exitReason, exitPct, conditions:[{expr,leftLabel,leftValue,op,rightLabel,rightValue,passed}]}. side는 BUY/SELL.
    3. ledger.buy(...)/ledger.sell(...) 호출에 rationaleJson 전달.
    4. buildResult()의 TradeRecord→TradeDto 매핑에 t.rationaleJson() 전달.
    5. ObjectMapper는 기존 주입 빈 재사용(없으면 필드로 주입). JsonProcessingException은 기존 예외 처리 컨벤션 따라 처리.
  </action>
  <verify>
    <automated>./gradlew :backend:compileJava</automated>
  </verify>
  <done>엔진이 evalEntry/evalExit 포착, buildRationale로 JSON 생성, ledger와 TradeDto까지 rationaleJson 전파. compileJava 성공.</done>
</task>

<task type="auto">
  <name>Task 3: BacktestServiceIntradayTest에 rationale 케이스 추가</name>
  <files>backend/src/test/java/com/graphify/trading/backtest/BacktestServiceIntradayTest.java</files>
  <action>
    VALIDATION map: "BacktestResult.TradeDto에 rationaleJson 포함" (파일 존재, 케이스 추가).
    기존 테스트 픽스처 재사용해 진입 신호가 발생하는 백테스트를 실행하고:
    - 결과 trades의 매수 TradeDto.rationaleJson()이 non-null이고 파싱 가능한 JSON.
    - JSON에 side="BUY"와 conditions 배열(각 expr/leftValue/passed 포함)이 존재.
    - 청산이 발생하는 시나리오에서 매도 TradeDto.rationaleJson에 exitReason이 TAKE_PROFIT/STOP_LOSS/INDICATOR 중 하나로 채워짐.
    ObjectMapper로 역직렬화해 필드 단언.
  </action>
  <verify>
    <automated>./gradlew :backend:test --tests "*.BacktestServiceIntradayTest"</automated>
  </verify>
  <done>백테스트 매수/매도 TradeDto에 근거 JSON이 채워짐을 자동 검증. green.</done>
</task>

</tasks>

<verification>
- `./gradlew :backend:test --tests "com.graphify.trading.*" -x integrationTest` green
- 백테스트 trade ↔ 근거가 인라인으로 연결됨 (조인/인덱스 매칭 불필요)
- rationale JSON 스키마가 RESEARCH Discretion 2와 일치 (Plan 04 모의와 동일)
</verification>

<success_criteria>
- 백테스트 각 거래 행에 트리거 근거(조건식+값+✓/✗, 청산 사유)가 JSON으로 포함됨 (SC4)
- 모의(Plan 04)와 동일 JSON 스키마
</success_criteria>

<output>
완료 후 `.planning/phases/06.5-role-split-trade-rationale/06.5-03-SUMMARY.md` 생성. rationale JSON 정확한 필드명/형태를 Plan 05 프론트 파서가 참조하도록 기록.
</output>
