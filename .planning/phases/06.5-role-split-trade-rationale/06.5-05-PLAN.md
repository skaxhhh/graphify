---
phase: 06.5-role-split-trade-rationale
plan: 05
type: execute
wave: 3
depends_on: ["06.5-01", "06.5-03", "06.5-04"]
files_modified:
  - frontend/src/types/trading.ts
  - frontend/src/types/paper.ts
  - frontend/src/lib/paperApi.ts
  - frontend/src/components/trading/TradeRationaleRow.tsx
  - frontend/src/pages/trading/paper/PaperRulesPage.tsx
  - frontend/src/pages/trading/TradingRulesPage.tsx
  - frontend/src/pages/trading/paper/PaperBacktestPage.tsx
  - frontend/src/pages/trading/paper/PaperHistoryPage.tsx
autonomous: false
requirements: [RULE-08, RULE-09]
must_haves:
  truths:
    - "전략 설정(/paper/rules)은 모든 상태 룰을 config 배지(DRAFT/ACTIVE) + ACTIVE 토글로만 제어하고 시작/중지를 노출하지 않는다"
    - "전략 운영(/paper/rules-lifecycle)은 ACTIVE 룰만 run 배지(실행 중/중지됨) + 시작/중지로만 제어한다"
    - "두 화면이 동일 상태를 중복 표시하지 않는다"
    - "백테스트와 모의 거래 행 클릭 시 근거 아코디언(조건식+지표값+✓/✗, 청산 사유)이 펼쳐진다"
  artifacts:
    - path: "frontend/src/components/trading/TradeRationaleRow.tsx"
      provides: "백테스트/모의 공유 근거 확장 행 컴포넌트"
      contains: "TradeRationaleRow"
    - path: "frontend/src/pages/trading/paper/PaperRulesPage.tsx"
      provides: "config 배지 + ACTIVE 토글, run 제어 제거"
      contains: "configStatus"
    - path: "frontend/src/pages/trading/TradingRulesPage.tsx"
      provides: "ACTIVE 필터 + run 배지 + start/stop"
      contains: "runStatus"
  key_links:
    - from: "PaperRulesPage / TradingRulesPage"
      to: "paperApi activate/start/stop"
      via: "useMutation + invalidateQueries"
      pattern: "activateRule|startRule|stopRule"
    - from: "PaperBacktestPage / PaperHistoryPage"
      to: "TradeRationaleRow"
      via: "행 클릭 expandedId 아코디언"
      pattern: "TradeRationaleRow|expandedId"
---

<objective>
프론트엔드를 2축 상태에 맞춰 분리하고 근거 아코디언을 추가한다: 전략 설정(PaperRulesPage)은 config축만, 전략 운영(TradingRulesPage)은 ACTIVE 룰의 run축만 제어한다. 백테스트/모의 거래 테이블 양쪽에 공유 TradeRationaleRow 확장 행을 삽입한다.

Purpose: RULE-08(역할 분리 UI, SC1-3)과 RULE-09(근거 표시 UI, SC4-5)를 사용자에게 전달. Plan 01/03/04의 백엔드 위에서 화면을 완성한다.
Output: 2축 타입, 새 API 함수, TradeRationaleRow 공유 컴포넌트, 분리된 두 룰 페이지, 근거 아코디언이 적용된 백테스트/모의 이력 페이지.
</objective>

<execution_context>
@/Users/heojeonghun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heojeonghun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.5-role-split-trade-rationale/06.5-RESEARCH.md
@.planning/phases/06.5-role-split-trade-rationale/06.5-01-SUMMARY.md
@.planning/phases/06.5-role-split-trade-rationale/06.5-03-SUMMARY.md
@.planning/phases/06.5-role-split-trade-rationale/06.5-04-SUMMARY.md

@frontend/src/types/trading.ts
@frontend/src/types/paper.ts
@frontend/src/lib/paperApi.ts
@frontend/src/pages/trading/paper/PaperRulesPage.tsx
@frontend/src/pages/trading/TradingRulesPage.tsx
@frontend/src/pages/trading/paper/PaperHistoryPage.tsx
@frontend/src/pages/trading/paper/PaperBacktestPage.tsx

<interfaces>
<!-- Extracted from codebase. Backend endpoint paths/field names confirmed in Plan 01/03/04 SUMMARYs. -->

Current types (trading.ts):
  TradingRule { id, name, mode, status, backtested, definition, promotedFrom, createdAt, updatedAt }
  BacktestTrade { datetime, symbol, companyName, side, qty, price, pnl }
  → add configStatus:"DRAFT"|"ACTIVE", runStatus:"STOPPED"|"RUNNING" to TradingRule; add rationaleJson:string|null to BacktestTrade.

Current types (paper.ts):
  PaperTradeHistoryItem { id, tradedAt, symbol, side, qty, price, fee, pnl }
  → add rationaleJson:string|null.

Current paperApi.ts (replace lifecycle calls):
  promoteRule/pauseRule/resumeRule/copyRule POST to /promote /pause /resume /copy.
  → add activateRule(id)→/activate, deactivateRule(id)→/deactivate, startRule(id)→/start, stopRule(id)→/stop. Keep copyRule.

router (already wired): /paper/rules→PaperRulesPage, /paper/rules-lifecycle→TradingRulesPage, /paper/...→PaperBacktestPage & PaperHistoryPage.

rationale JSON shape (from Plan 03/04, RESEARCH Discretion 2 / E5):
  { side:"BUY"|"SELL", exitReason:"TAKE_PROFIT"|"STOP_LOSS"|"INDICATOR"|null, exitPct:number|null,
    conditions:[{expr,leftLabel,leftValue,op,rightLabel,rightValue,passed}] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 타입 확장 + paperApi 새 lifecycle 함수 + TradeRationaleRow 공유 컴포넌트</name>
  <files>frontend/src/types/trading.ts, frontend/src/types/paper.ts, frontend/src/lib/paperApi.ts, frontend/src/components/trading/TradeRationaleRow.tsx</files>
  <action>
    1. trading.ts: TradingRule에 configStatus:"DRAFT"|"ACTIVE", runStatus:"STOPPED"|"RUNNING" 추가. BacktestTrade에 rationaleJson:string|null 추가. (기존 status는 호환 위해 유지.)
    2. paper.ts: PaperTradeHistoryItem에 rationaleJson:string|null 추가.
    3. paperApi.ts: activateRule(id)→POST /activate, deactivateRule(id)→/deactivate, startRule(id)→/start, stopRule(id)→/stop 추가 (기존 함수 패턴 그대로). copyRule 유지. (promote/pause/resume는 더 이상 호출 안 하면 제거 가능하나, 안전하게 남겨도 됨 — 페이지에서 교체하는 것이 핵심.)
    4. components/trading/TradeRationaleRow.tsx 생성 (RESEARCH E5 패턴): props { rationale: TradeRationale }. TradeRationale 인터페이스(side, exitReason, exitPct, conditions[])는 이 파일 또는 공유 타입에 정의. 렌더: 청산 사유 라벨(익절 emerald / 손절 red / 지표 gray) + 각 condition을 `{leftLabel}={leftValue.toFixed(2)} {op} {rightValue.toFixed(2)}` + ✓(emerald)/✗(red)로 표시. rationale null/빈 배열 방어.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>타입 확장 + 4개 새 API 함수 + TradeRationaleRow 컴포넌트 존재. tsc --noEmit 통과.</done>
</task>

<task type="auto">
  <name>Task 2: 전략 설정(PaperRulesPage) config축 분리 + 전략 운영(TradingRulesPage) run축 분리</name>
  <files>frontend/src/pages/trading/paper/PaperRulesPage.tsx, frontend/src/pages/trading/TradingRulesPage.tsx</files>
  <action>
    RESEARCH Discretion 7:
    PaperRulesPage (전략 설정):
      - 배지를 rule.configStatus(DRAFT/ACTIVE)로 표시 (status 배지 교체).
      - ACTIVE 토글 버튼: DRAFT→activateRule / ACTIVE→deactivateRule. deactivate는 runStatus==="RUNNING"이면 비활성화(먼저 운영에서 중지 안내).
      - 시작/중지(run 제어) 버튼 완전 제거.
      - 편집 버튼: configStatus==="ACTIVE" && runStatus==="RUNNING"이면 비활성화.
      - useMutation + onSuccess invalidateQueries 패턴.
    TradingRulesPage (전략 운영):
      - rules.filter(r => r.configStatus === "ACTIVE")로 ACTIVE만 표시. DRAFT는 안 보임.
      - STATUS_CONFIG 배지를 run 배지로 재구성: runStatus==="RUNNING"→"실행 중"(emerald), "STOPPED"→"중지됨"(yellow).
      - promote/pause/resume 버튼을 startRule/stopRule로 교체: STOPPED→시작, RUNNING→중지.
      - "마지막 신호 시각" 컬럼 추가 (Plan 01 SUMMARY의 lifecycle DTO 또는 기존 signal log max(ts) 소스 사용; 백엔드가 제공하지 않으면 가능 범위에서 생략하고 SUMMARY에 명시).
      - 사이드바/메뉴 라벨이 "전략 설정"/"전략 운영"인지 확인(아니면 정정).
    중복 제거 검증: 설정=config축만, 운영=run축만 (SC3).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>설정은 config 배지+ACTIVE 토글(시작/중지 없음), 운영은 ACTIVE 필터+run 배지+시작/중지. RUNNING 중 편집/하향 비활성화. tsc 통과.</done>
</task>

<task type="auto">
  <name>Task 3: 백테스트/모의 거래 테이블에 근거 아코디언 삽입</name>
  <files>frontend/src/pages/trading/paper/PaperBacktestPage.tsx, frontend/src/pages/trading/paper/PaperHistoryPage.tsx</files>
  <action>
    RESEARCH E4 패턴, 양쪽 공통:
    1. const [expandedId, setExpandedId] = useState<number|string|null>(null).
    2. 거래 행에 onClick={() => setExpandedId(expandedId===t.id ? null : t.id)} + cursor-pointer + hover. 마지막 셀에 ▼/▲ 토글 인디케이터.
    3. expandedId===t.id && t.rationaleJson일 때 확장 <tr> 렌더: colSpan으로 전체 폭, <TradeRationaleRow rationale={JSON.parse(t.rationaleJson)} />. JSON.parse 실패 방어(try/catch 또는 헬퍼).
    4. PaperBacktestPage는 BacktestTrade.rationaleJson, PaperHistoryPage는 PaperTradeHistoryItem.rationaleJson 사용. 동일 TradeRationaleRow 재사용(SC4=SC5 동일 형식).
    5. rationaleJson이 null인 행은 클릭해도 확장 안 함(또는 "근거 없음" 표시).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>두 페이지 모두 행 클릭 아코디언으로 TradeRationaleRow 표시. tsc + build 성공.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 통합 육안 검증 — 역할 분리 + 근거 아코디언</name>
  <action>아래 how-to-verify 절차대로 두 화면 역할 경계(SC1-3)와 근거 아코디언(SC4-5)을 육안 확인한다.</action>
  <what-built>2축 역할 분리 + 근거 아코디언 전체 플로우 (Plan 01-05 통합). 백엔드 자동화 완료, 프론트 빌드 성공.</what-built>
  <how-to-verify>
    1. 앱 실행 후 "전략 설정"(/paper/rules) 방문: 모든 상태 룰이 나열되고 config 배지(DRAFT/ACTIVE)와 ACTIVE 토글만 보이며 시작/중지 버튼이 없는지 확인. DRAFT 룰을 ACTIVE로 토글(백테스트 없이 성공).
    2. "전략 운영"(/paper/rules-lifecycle) 방문: ACTIVE 룰만 보이고 DRAFT는 안 보이는지, run 배지(실행 중/중지됨)와 시작/중지 버튼만 있는지 확인. 룰 시작→실행 중, 중지→중지됨 전환 확인. RUNNING 룰이 설정 화면에서 편집/하향 비활성인지 확인.
    3. 두 화면이 동일 상태를 중복 표시하지 않는지 확인 (SC3).
    4. 백테스트 결과 거래 테이블에서 한 행 클릭 → 근거 아코디언 펼쳐짐: `RSI(14)=28.3 < 30.00 ✓` 형식 조건 + 청산 사유(익절/손절/지표) 표시 확인.
    5. 모의 거래 이력(PaperHistoryPage)에서 동일하게 행 클릭 → 동일 형식 근거 표시 확인.
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 발견한 이슈 설명</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit && npm run build` 성공
- VALIDATION Manual-Only: 두 화면 역할 경계 + 근거 아코디언 육안 확인 (체크포인트)
</verification>

<success_criteria>
- 전략 설정 = config축만, 전략 운영 = run축만, 중복 없음 (RULE-08 SC1-3)
- 백테스트 + 모의 거래 행 클릭 시 동일 형식 근거 아코디언 (RULE-09 SC4-5)
</success_criteria>

<output>
완료 후 `.planning/phases/06.5-role-split-trade-rationale/06.5-05-SUMMARY.md` 생성. DESIGN.md에 `## [v?.?.?] 룰 설정/운영 역할 분리 & 매매 근거` 항목 추가(최신 위로) + Status, RELEASE_NOTES.md 반영 (프로젝트 문서 규칙).
</output>
