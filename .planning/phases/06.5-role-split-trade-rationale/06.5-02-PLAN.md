---
phase: 06.5-role-split-trade-rationale
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/graphify/trading/engine/EvalResult.java
  - backend/src/main/java/com/graphify/trading/engine/RuleEvaluator.java
  - backend/src/test/java/com/graphify/trading/engine/RuleEvaluatorRichTest.java
autonomous: true
requirements: [RULE-09]
must_haves:
  truths:
    - "RuleEvaluator가 진입/청산 평가 시 충족 조건식 + 조건별 실제 지표값 + ✓/✗를 담은 EvalResult를 반환한다"
    - "청산 평가가 익절(TAKE_PROFIT)/손절(STOP_LOSS)/지표(INDICATOR) 사유를 구분해 반환한다"
    - "기존 boolean entryTriggered/exitTriggered 시그니처는 그대로 동작한다 (하위 호환)"
  artifacts:
    - path: "backend/src/main/java/com/graphify/trading/engine/EvalResult.java"
      provides: "리치 반환 타입 (conditions + exitReason + exitPct)"
      contains: "ExitReason"
    - path: "backend/src/main/java/com/graphify/trading/engine/RuleEvaluator.java"
      provides: "evalEntry/evalExit 리치 메서드"
      contains: "evalEntry"
    - path: "backend/src/test/java/com/graphify/trading/engine/RuleEvaluatorRichTest.java"
      provides: "조건별 값/passed + exitReason 구분 검증"
      contains: "RuleEvaluatorRichTest"
  key_links:
    - from: "RuleEvaluator.evalEntry"
      to: "EvalResult.ConditionResult"
      via: "조건별 leftValue/op/rightValue/passed 채움"
      pattern: "ConditionResult"
    - from: "RuleEvaluator.evalExit"
      to: "EvalResult.ExitReason"
      via: "TP/SL/INDICATOR 분기"
      pattern: "TAKE_PROFIT|STOP_LOSS|INDICATOR"
---

<objective>
RuleEvaluator의 boolean-only 평가를 `EvalResult` 리치 반환 타입으로 확장한다. 진입/청산 시 어떤 조건이 어떤 실제 값으로 충족됐는지(조건식 + leftValue/op/rightValue + ✓/✗)와 청산 사유(익절/손절/지표)를 포착한다.

Purpose: 매매 근거(RULE-09)의 핵심 데이터 소스. 백테스트(Plan 03)와 모의(Plan 04)가 동일한 EvalResult를 포착해 동일 JSON 스키마로 근거를 생성한다.
Output: EvalResult record, evalEntry/evalExit 메서드, 조건별 값/사유 검증 테스트.

TDD: 평가 입력→출력이 명확히 정의되므로 RED→GREEN 사이클로 구현한다.
</objective>

<execution_context>
@/Users/heojeonghun/.claude/get-shit-done/workflows/execute-plan.md
@/Users/heojeonghun/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06.5-role-split-trade-rationale/06.5-RESEARCH.md

@backend/src/main/java/com/graphify/trading/engine/RuleEvaluator.java
@backend/src/main/java/com/graphify/trading/engine/Indicators.java

<interfaces>
<!-- Extracted from codebase — use directly. -->

Current RuleEvaluator.java signatures (KEEP these — add overloads, do not remove):
```java
public boolean entryTriggered(ConditionGroup entry, double[] closes, Double[] volumes, int i)
public boolean exitTriggered(ExitSpec exit, double[] closes, Double[] volumes, int i, double entryPrice)
```

Target new record (RESEARCH Discretion 3):
```java
public record EvalResult(
    boolean triggered,
    List<ConditionResult> conditions,
    ExitReason exitReason,   // null = entry
    Double exitPct           // null if not TP/SL
) {
    public record ConditionResult(
        String expr, String leftLabel, double leftValue,
        String op, String rightLabel, double rightValue, boolean passed
    ) {}
    public enum ExitReason { TAKE_PROFIT, STOP_LOSS, INDICATOR }
}
```

Target new methods:
```java
public EvalResult evalEntry(ConditionGroup entry, double[] closes, Double[] volumes, int i)
public EvalResult evalExit(ExitSpec exit, double[] closes, Double[] volumes, int i, double entryPrice)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — RuleEvaluatorRichTest 작성 (실패 확인)</name>
  <files>backend/src/test/java/com/graphify/trading/engine/RuleEvaluatorRichTest.java</files>
  <behavior>
    - evalEntry: `RSI(14) < 30` 조건, closes 입력이 RSI=28.3을 만들 때 → triggered=true, conditions[0].leftLabel="RSI(14)", leftValue≈28.3, op="<", rightValue=30, passed=true.
    - evalEntry 다중 조건(AND): 한 조건 통과/한 조건 실패 시 → 각 ConditionResult의 passed가 개별 보존되고, 그룹 logic=AND면 triggered=false.
    - evalExit 익절: entryPrice 대비 현재가 +X% ≥ takeProfitPct → triggered=true, exitReason=TAKE_PROFIT, exitPct≈+X.
    - evalExit 손절: -X% ≤ stopLossPct → exitReason=STOP_LOSS, exitPct≈-X.
    - evalExit 지표 청산: TP/SL 미충족 + 청산 지표 조건 충족 → exitReason=INDICATOR, exitPct=null.
    - crossAbove 조건(Pitfall 3): leftValue=lNow, rightValue=rNow (현재 인덱스), expr에 "crossAbove" 포함.
    - 하위 호환: 기존 entryTriggered/exitTriggered가 동일 입력에서 evalEntry/evalExit의 triggered와 일치.
  </behavior>
  <action>
    JUnit5 + Mockito 슬라이스(기존 engine 테스트 참조). 위 behavior를 단언하는 RuleEvaluatorRichTest를 먼저 작성한다. EvalResult/evalEntry/evalExit가 아직 없으므로 컴파일/실행이 실패(RED)함을 확인한다. ConditionGroup/ExitSpec 픽스처는 기존 RuleEvaluator 테스트 패턴 재사용.
  </action>
  <verify>
    <automated>./gradlew :backend:compileTestJava || echo "RED expected — compile fails until Task 2"</automated>
  </verify>
  <done>RuleEvaluatorRichTest 파일 존재, 위 케이스 단언 포함. EvalResult/evalEntry/evalExit 부재로 RED 상태.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — EvalResult + RuleEvaluator evalEntry/evalExit 구현</name>
  <files>backend/src/main/java/com/graphify/trading/engine/EvalResult.java, backend/src/main/java/com/graphify/trading/engine/RuleEvaluator.java</files>
  <behavior>
    Task 1의 모든 케이스가 GREEN이 되어야 한다. 기존 boolean 시그니처 회귀 없음.
  </behavior>
  <action>
    1. EvalResult.java 생성 (context interface 스키마: triggered, conditions[], exitReason, exitPct + ConditionResult record + ExitReason enum).
    2. RuleEvaluator에 evaluateGroupRich(group, closes, volumes, i): 각 Condition을 평가하며 ConditionResult 생성. expr = leftLabel + " " + op + " " + rightLabel. 지표 라벨은 기존 지표 종류(PRICE/SMA/EMA/RSI/VOLUME + 기간)에서 생성. crossAbove/crossBelow는 RESEARCH Pitfall 3대로 현재 인덱스 값 기록.
    3. applyLogic(group.logic(), results): AND/OR 합성.
    4. evalEntry → new EvalResult(triggered, results, null, null).
    5. evalExit: 먼저 TP/SL 가격 변동률 체크(exitReason+exitPct 설정), 미충족 시 지표 조건 평가(INDICATOR). 기존 exitTriggered의 TP/SL/지표 판정 로직을 재사용하되 reason 보존.
    6. 기존 boolean entryTriggered/exitTriggered는 evalEntry().triggered()/evalExit().triggered()로 위임.
    REFACTOR: 중복 조건 평가 로직을 private helper로 정리.
  </action>
  <verify>
    <automated>./gradlew :backend:test --tests "*.RuleEvaluatorRichTest" --tests "com.graphify.trading.engine.*"</automated>
  </verify>
  <done>Task 1 케이스 전부 green, 기존 engine 테스트 회귀 없음.</done>
</task>

</tasks>

<verification>
- `./gradlew :backend:test --tests "*.RuleEvaluatorRichTest"` green
- 기존 RuleEvaluator 의존 테스트 회귀 없음: `./gradlew :backend:test --tests "com.graphify.trading.engine.*"`
</verification>

<success_criteria>
- evalEntry/evalExit가 조건별 실제 값 + passed를 보존 (그룹 boolean만으로 부족한 부분 해결)
- exitReason이 TAKE_PROFIT/STOP_LOSS/INDICATOR 3분기로 구분됨
- 기존 boolean 시그니처 하위 호환 유지
</success_criteria>

<output>
완료 후 `.planning/phases/06.5-role-split-trade-rationale/06.5-02-SUMMARY.md` 생성. EvalResult 필드 구조와 buildRationale에 쓸 JSON 매핑 의도를 Plan 03/04가 참조하도록 기록.
</output>
