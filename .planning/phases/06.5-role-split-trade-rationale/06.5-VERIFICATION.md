---
phase: 06.5-role-split-trade-rationale
verified: 2026-06-22T11:05:00Z
status: human_needed
score: 5/5 success-criteria code-verified
re_verification:
  previous_status: gaps_found
  previous_score: 4/5
  gaps_closed:
    - "스케줄러/평가 게이트(isLiveActive)가 run_status=RUNNING 기준으로 동작한다 — both LiveEvaluationService:95 and PaperLiveSymbolService:56 now route through isLiveActive(TradingRule); test fixture status='DRAFT' so it passes only via run_status gate. Confirmed against actual code (commit 8c7bfdb), test suite green."
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Full UI role separation walkthrough"
    expected: >
      전략 설정(/paper/rules): all rules shown with DRAFT/ACTIVE config badge;
      activate/deactivate buttons only; no start/stop buttons visible.
      전략 운영(/paper/rules-lifecycle): only ACTIVE rules shown; run badge
      (실행 중 / 중지됨); start/stop buttons only. RUNNING rule edit/deactivate
      disabled in settings screen.
    why_human: "Visual layout, button state, and inter-screen state consistency cannot be verified by grep"
  - test: "Rationale accordion UI — backtest"
    expected: >
      Click a trade row in backtest results → accordion expands showing
      condition rows e.g. RSI(14)=28.3 < 30.00 ✓ with exit reason badge for SELL rows.
    why_human: "DOM rendering and interactive expand behavior require browser"
  - test: "Rationale accordion UI — paper history (end-to-end)"
    expected: >
      With a rule activated via /activate then /start (config=ACTIVE, run=RUNNING),
      the live loop now evaluates it (gate fixed), generates signal logs with merged
      rationale, and the paper history page row click shows the same TradeRationaleRow format.
    why_human: "Requires a live tick cycle producing paper trades + matching signal_log rows; runtime behavior not grep-verifiable"
---

# Phase 6.5: Role Split & Trade Rationale Verification Report

**Phase Goal:** 모의 룰 설정 화면(CRUD + DRAFT↔ACTIVE)과 룰 라이프사이클 화면(ACTIVE 룰만 + PAPER_LIVE 시작/중지)의 역할을 명확히 분리하고, 백테스트·모의 매매 이력의 각 매수/매도에 트리거 근거(충족 조건식 + 그 시점 지표값 + 청산 사유)를 표시한다.
**Verified:** 2026-06-22T11:05:00Z (re-verification after gap closure)
**Status:** human_needed
**Re-verification:** Yes — after gap closure (commit 8c7bfdb). Previous: gaps_found 4/5.

---

## Re-Verification Summary

The single BLOCKER gap from the initial verification (live evaluation/ingest gate keyed on the deprecated legacy-status overload instead of the new `run_status='RUNNING'` overload) has been **closed and confirmed against actual code**:

| File | Before | After (commit 8c7bfdb) | Confirmed |
|------|--------|------------------------|-----------|
| `LiveEvaluationService.java:95` | `.filter(r -> RuleStatus.isLiveActive(r.getStatus()))` | `.filter(RuleStatus::isLiveActive)` | grep verified — method reference resolves to `isLiveActive(TradingRule)` (the `TradingRule` stream element cannot bind to the `String` overload) |
| `PaperLiveSymbolService.java:56` | `.filter(r -> RuleStatus.isLiveActive(r.getStatus()))` | `.filter(RuleStatus::isLiveActive)` | grep verified — same |
| `LiveEvaluationServiceTest.java:73` | `new TradingRule(..., "PAPER_LIVE", ...)` (masked gate) | `new TradingRule(..., "DRAFT", ...)` (line 73) + `setRunStatus("RUNNING")` (line 75), with comment explaining rule passes ONLY via run_status gate | grep verified — masking resolved; test now isolates the new gate |

`cd backend && ./gradlew test --tests "com.graphify.trading.*"` → **BUILD SUCCESSFUL** (re-run during this re-verification, exit code 0).

All five success criteria are now code-verified. The remaining items are visual/runtime UI behaviors that cannot be verified programmatically — hence `human_needed` rather than `passed`. (Project git history records these as user-approved during execution at commit `b08905f` "Task 4 visual verify approved (SC1-5)"; this verifier does not treat a relayed/recorded approval as a substitute for its own programmatic check, so the visual items remain listed for human confirmation.)

---

## Goal Achievement

### Observable Truths (from Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | PaperRulesPage shows all-status rules + DRAFT↔ACTIVE toggle only, no run control | VERIFIED | `PaperRulesPage.tsx`: ConfigBadge on configStatus, activateMutation/deactivateMutation wired, no startRule/stopRule import or call |
| 2 | TradingRulesPage shows ACTIVE rules only + run badge + start/stop | VERIFIED | `TradingRulesPage.tsx:34-36`: filter `configStatus === "ACTIVE"`; RunBadge(RUNNING/STOPPED); startMutation/stopMutation; no activate/deactivate |
| 3 | Two screens do not duplicate state, role boundary is clear | VERIFIED | Settings header "전략 설정" + "실행 제어는 전략 운영 화면에서"; Ops "전략 운영" + "설정 변경은 전략 설정 화면에서" |
| 4 | Backtest trade history shows trigger rationale per trade (condition + indicator value) | VERIFIED | `IntradayBacktestEngine.java:132-147`: evalEntry/evalExit captured, buildRationale called, rationaleJson in TradeDto; `PaperBacktestPage.tsx:216-267`: expandedId accordion + TradeRationaleRow |
| 5 | Paper (PAPER_LIVE) trade history shows same signal rationale | VERIFIED | `LiveEvaluationService.java:173-183`: evalEntry/evalExit captured, mergeRationale wired; `PaperHistoryService.java:37-40`: JOIN via findFirstByRuleIdAndSymbolAndTsAndSignal; `PaperHistoryPage.tsx:64-115`: expandedId accordion + TradeRationaleRow. **Live loop gate now correctly keyed on run_status='RUNNING' (gap closed)** — rules activated via the 2-axis lifecycle are now actually evaluated and produce rationale-bearing signal logs |

**Score:** 5/5 success criteria code-verified. Visual/runtime UI confirmation deferred to human checks below.

---

## Required Artifacts

### Plan 01 Artifacts (2-Axis Status Model)

| Artifact | Status | Evidence |
|----------|--------|---------|
| `backend/src/main/resources/db/migration/V35__two_axis_status.sql` | VERIFIED | EXISTS; `ADD COLUMN IF NOT EXISTS config_status`, `ADD COLUMN IF NOT EXISTS run_status`, 3 UPDATE statements, `CREATE INDEX idx_trading_rules_run_status` |
| `backend/src/main/java/com/graphify/trading/rule/TradingRule.java` | VERIFIED | `configStatus` (line 36), `runStatus` (line 39) + getters/setters |
| `backend/src/main/java/com/graphify/trading/paper/PaperLifecycleService.java` | VERIFIED | `activate()`, `deactivate()`, `start()`, `stop()`, `listActive()`; deactivate guards RUNNING (ERR_LIFECYCLE_006); start guards non-ACTIVE |
| `backend/src/test/java/com/graphify/trading/rule/PaperLifecycleServiceTest.java` | VERIFIED | Trading test suite BUILD SUCCESSFUL |

### Plan 02 Artifacts (EvalResult Rich Return)

| Artifact | Status | Evidence |
|----------|--------|---------|
| `backend/src/main/java/com/graphify/trading/engine/EvalResult.java` | VERIFIED | Record with `triggered`, `conditions`, `exitReason`, `exitPct`; `ConditionResult` inner record (7 fields); `ExitReason` enum TAKE_PROFIT/STOP_LOSS/INDICATOR |
| `backend/src/main/java/com/graphify/trading/engine/RuleEvaluator.java` | VERIFIED | `evalEntry()` line 50, `evalExit()` line 67; `evaluateGroupRich`/`evaluateConditionRich`/`applyLogic`; old boolean methods delegate (lines 32,40) |
| `backend/src/test/java/com/graphify/trading/engine/RuleEvaluatorRichTest.java` | VERIFIED | Test suite green |

### Plan 03 Artifacts (Backtest Rationale Carrier)

| Artifact | Status | Evidence |
|----------|--------|---------|
| `backend/src/main/java/com/graphify/trading/engine/PaperLedger.java` | VERIFIED | `TradeRecord.rationaleJson`; `buy()`/`sell()` 5-arg overloads |
| `backend/src/main/java/com/graphify/trading/backtest/dto/BacktestResult.java` | VERIFIED | `TradeDto.rationaleJson` (line 29) |
| `backend/src/main/java/com/graphify/trading/backtest/IntradayBacktestEngine.java` | VERIFIED | Lines 132-147 capture evalExit/evalEntry, buildRationale, ledger.sell/buy with rationaleJson; line 255 passes to TradeDto |

### Plan 04 Artifacts (Paper Live Rationale + MON-05)

| Artifact | Status | Evidence |
|----------|--------|---------|
| `backend/src/main/java/com/graphify/trading/paper/LiveEvaluationService.java` | VERIFIED | evalExit (173)/evalEntry (180); mergeRationale called (176/183), defined (203); **gate at line 95 now `RuleStatus::isLiveActive` (run_status=RUNNING)** |
| `backend/src/main/java/com/graphify/trading/paper/dto/PaperTradeHistoryItem.java` | VERIFIED | `rationaleJson` 9th field |
| `backend/src/main/java/com/graphify/trading/paper/PaperExecutor.java` | VERIFIED | `PRICE_LIMIT_THRESHOLD = 0.295` (30); `isPriceLimitPending()` (69); `saveSignalLog(..., "PENDING", ..., false)` (71) |
| `backend/src/main/java/com/graphify/trading/paper/PaperHistoryService.java` | VERIFIED | `findFirstByRuleIdAndSymbolAndTsAndSignal(...)` (39); rationaleJson from matched log (40) |

### Plan 05 Artifacts (Frontend)

| Artifact | Status | Evidence |
|----------|--------|---------|
| `frontend/src/components/trading/TradeRationaleRow.tsx` | VERIFIED | Exports TradeRationale, ConditionResult, parseRationale (dual-format), TradeRationaleRow with exit badges + condition rows |
| `frontend/src/pages/trading/paper/PaperRulesPage.tsx` | VERIFIED | ConfigBadge; activate/deactivate; no start/stop; deactivate disabled on RUNNING; edit disabled on ACTIVE+RUNNING |
| `frontend/src/pages/trading/TradingRulesPage.tsx` | VERIFIED | filter configStatus==="ACTIVE"; RunBadge; start/stop; no activate/deactivate |
| `frontend/src/pages/trading/paper/PaperBacktestPage.tsx` | VERIFIED | expandedId; parseRationale; accordion tr with TradeRationaleRow; ▼/▲ |
| `frontend/src/pages/trading/paper/PaperHistoryPage.tsx` | VERIFIED | expandedId; parseRationale; accordion with TradeRationaleRow; hasRationale guard |

---

## Key Link Verification

| From | To | Via | Status | Evidence |
|------|----|-----|--------|---------|
| `RuleStatus.isLiveActive(TradingRule)` | `TradingRule.runStatus` | `"RUNNING".equals(rule.getRunStatus())` | VERIFIED | `RuleStatus.java:31` |
| `PaperLifecycleController` | `PaperLifecycleService.activate/start/stop` | POST endpoint mapping | VERIFIED | Controller lines 24-49: /activate, /deactivate, /start, /stop, /copy |
| `IntradayBacktestEngine` | `RuleEvaluator.evalEntry/evalExit` | EvalResult capture | VERIFIED | Lines 132,143 |
| `IntradayBacktestEngine` | `PaperLedger.buy/sell` | rationaleJson param | VERIFIED | Lines 135,147 |
| `PaperHistoryService` | `paper_signal_log` | `findFirstByRuleIdAndSymbolAndTsAndSignal` JOIN | VERIFIED | Line 39 |
| `LiveEvaluationService` | `PaperExecutor.execute` | rationale-merged indicatorSnapshotJson | VERIFIED | mergeRationale → executor.execute(..., snapshotJson) line 194 |
| `LiveEvaluationService.evaluateTick` | `RuleStatus.isLiveActive(TradingRule)` | run_status=RUNNING gate | **VERIFIED** (was BROKEN) | Line 95 now `.filter(RuleStatus::isLiveActive)` — method reference binds to TradingRule overload; test fixture status="DRAFT" + runStatus="RUNNING" proves run_status drives inclusion (commit 8c7bfdb) |
| `PaperLiveSymbolService.getActiveSymbols` | `RuleStatus.isLiveActive(TradingRule)` | run_status=RUNNING gate | **VERIFIED** (was BROKEN) | Line 56 now `.filter(RuleStatus::isLiveActive)` (commit 8c7bfdb) |
| `PaperRulesPage / TradingRulesPage` | `paperApi activate/start/stop` | useMutation + invalidateQueries | VERIFIED | paperApi.ts: activateRule(39), deactivateRule(43), startRule(47), stopRule(51); consumed in pages |
| `PaperBacktestPage / PaperHistoryPage` | `TradeRationaleRow` | expandedId accordion | VERIFIED | Both import TradeRationaleRow + parseRationale; expandedId controls accordion |

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| RULE-08 | Plans 01, 05 | 모의 룰 설정/운영 화면 역할 분리 (config축/run축) | SATISFIED | 2-axis DB (V35), lifecycle service, frontend role split |
| RULE-09 | Plans 02, 03, 04, 05 | 매매 근거 백테스트/모의 표시 | SATISFIED | EvalResult, buildRationale/mergeRationale, TradeDto/PaperTradeHistoryItem rationaleJson, TradeRationaleRow; live loop gate fix means RUNNING rules now actually generate rationale end-to-end |
| MON-05 | Plan 04 | 상한가/하한가 PRICE_LIMIT_PENDING | SATISFIED | PaperExecutor.isPriceLimitPending() 29.5%, PENDING log, TradeResult.skipped |

**Note on RULE-08/RULE-09 in REQUIREMENTS.md:** These IDs do not appear in the v1 requirements table or the Traceability table (which ends at RULE-05). They are phase-6.5 planning requirements added to v2 scope. The absence from REQUIREMENTS.md Traceability is a documentation gap, not an implementation gap. MON-05 appears at line 95 under v2.

---

## Anti-Patterns Found

| File | Pattern | Severity | Status |
|------|---------|----------|--------|
| `LiveEvaluationService.java:95` | (former) deprecated isLiveActive(String) gate | — | **RESOLVED** in commit 8c7bfdb — now `RuleStatus::isLiveActive` (TradingRule overload) |
| `PaperLiveSymbolService.java:56` | (former) deprecated isLiveActive(String) gate | — | **RESOLVED** in commit 8c7bfdb — now `RuleStatus::isLiveActive` |
| `LiveEvaluationServiceTest.java:73` | (former) fixture status='PAPER_LIVE' masking gate | — | **RESOLVED** — fixture now status='DRAFT' + runStatus='RUNNING'; isolates new gate |

No outstanding anti-patterns in phase-6.5 scope.

**Pre-existing flaky test (not phase 6.5):** The full `./gradlew test` suite has 4 failures in `com.graphify.market.LiveDataSchedulerTest`. These invoke the scheduler against the real wall clock; Guard 1 in `LiveDataScheduler.collectLiveData()` early-returns after 15:30 KST (MARKET_CLOSE), so stubs go unused (UnnecessaryStubbingException / "wanted but not invoked"). No phase-6.5 commit touched the scheduler, its test, or PaperLiveSymbolService's scheduler path. Not counted against the phase goal — noted as pre-existing flaky test only.

---

## Human Verification Required

### 1. Role Separation UI

**Test:** Start the app. Visit /trading/paper/rules (전략 설정) — verify all rules show DRAFT/ACTIVE config badge with activate/deactivate only, no start/stop. Visit /trading/paper/rules-lifecycle (전략 운영) — verify only ACTIVE rules show with run badge (실행 중/중지됨) and start/stop only. Confirm RUNNING rule edit/deactivate disabled in settings.
**Expected:** Clear role boundary; no duplicated state.
**Why human:** Visual layout and interactive button states are not grep-verifiable.

### 2. Rationale Accordion — Backtest

**Test:** Run a backtest on an RSI/SMA rule; click a trade row.
**Expected:** Accordion expands with condition rows like `RSI(14)=28.3 < 30.00 ✓`; SELL rows show exit reason (익절/손절/지표).
**Why human:** Requires backtest data + DOM interaction.

### 3. Rationale Accordion — Paper History (end-to-end, now unblocked)

**Test:** Activate a rule via /activate then /start (config=ACTIVE, run=RUNNING). Let the live loop tick. Visit paper history; click a trade row.
**Expected:** With the gate fix, the rule is now evaluated, signal logs carry merged rationale, and the row shows the same TradeRationaleRow format as backtest.
**Why human:** Requires a runtime tick cycle producing paper trades + matching signal_log rows.

---

## Gaps Summary

**No remaining gaps.** The single structural gap from initial verification — the live evaluation and ingest gate using the deprecated legacy-status overload — is closed (commit 8c7bfdb) and confirmed against actual source: both `LiveEvaluationService:95` and `PaperLiveSymbolService:56` now route through `RuleStatus::isLiveActive` (the `TradingRule` / `run_status='RUNNING'` overload), and `LiveEvaluationServiceTest.runningRule()` now constructs with legacy `status='DRAFT'` so the test passes only via the new gate. Trading test suite re-run: BUILD SUCCESSFUL.

All five success criteria are code-verified (SC1-SC4 fully; SC5 infrastructure fully wired and the live loop now actually reaches RUNNING rules). The only items not programmatically verifiable are the three visual/runtime UI behaviors listed above — hence overall status `human_needed`.

---

_Verified: 2026-06-22T11:05:00Z (re-verification)_
_Verifier: Claude (gsd-verifier)_
